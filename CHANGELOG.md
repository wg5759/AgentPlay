# 变更日志

本项目遵循“已发布版本、候选版本、未来计划分开记录”的原则。未进入 GitHub Release 的内容不得写成公开已交付。

## [0.9.1-preview.2] - 2026-08-29

### 修复

- 修复 Windows 原生全屏与界面影院状态不同步后，`Esc` 或双击无法退出全屏的问题；全屏操作改为显式目标状态，主进程增加原生 Escape 兜底。
- 修复“最近记录”在存在任务时只展示 4 条播放记录的问题；播放记录现在独立完整显示，最多保留 30 条。
- 修复 Electron 43 拖入媒体仍读取已移除的 `File.path`、从而写入无路径坏记录的问题；改用 `webUtils.getPathForFile`，并迁移清洗既有坏记录。

### 验证

- 完整源码回归 1018/1018 通过；TypeScript、ESLint 与 Windows 解包候选构建通过。
- 安装态全屏七阶段验收覆盖双击进入、Escape 退出、双击退出及原生/界面分歧状态恢复。
- 安装态播放历史验收覆盖旧 schema 迁移、有任务时完整展示，以及真实历史持久数与可见数一致。

### 已知限制

- 本版本继续只作为明确标注 `NotSigned` 的 GitHub Prerelease；Windows SmartScreen 可能显示“未知发布者”。
- Stable 仍等待免费开源代码签名计划获批及安装器、主程序双重 Authenticode 验收。

## [0.9.1-preview.1] - 2026-08-27

### 新增

- 自然语言专业影音编辑 Agent：语义剪辑、内容定位、自动体检、长转短、多平台版本、智能构图、画面效果、专业字幕、多轨音频、音频修复、节拍剪辑、批量编辑、个人风格 Skill 与专业素材库质量标定。
- 项目胶囊与统一成果工作流：跨视频、音频、网页、PDF、Office、图片和字幕持续工作，保留来源、版本、证据、审批、恢复和质量回执。
- 英文主 README、中文独立 README、真实安装态演示视频、GitHub Pages 落地页和 1280×640 社交预览图。

### 验证

- 0.9.1 固定规划 25/25 项具备实现、自动回归、适用安装态验收和证据。
- E5 专业编辑语料 20/20 通过且质量评分均为 100；8 个重启样本未重复已完成步骤，云端调用为 0。
- Windows 安装态候选通过播放器、下载、文档、模型路由、字幕、编辑、恢复与插件权限验收；Ubuntu/Windows CI 继续作为源码质量门。

### 已知限制

- 本版本只允许发布为 GitHub Prerelease，并明确标记 `NotSigned`；Windows SmartScreen 可能显示“未知发布者”。
- macOS、Linux、Android 与 iOS 尚未达到 Windows 同等级的公开安装闭环。
- Facebook 等登录态站点仍需要用户自己的有效 Cookies；没有登录态时明确失败，不伪造成功。

## [0.8.0] - Unreleased

### 新增

- 全新的 AgentPlay AI 原生工作区与飞鸽品牌界面：能力收进对话、活动轨和按需抽屉，首页不再常驻工程化配置面板。
- “问答 / 规划 / 执行 / 自动”四种模型无关工作模式，统一覆盖 Agnes、OpenAI、Claude、Gemini、本地模型和订阅 CLI。
- 16 项播放器工具的单一注册表、工具参数校验、风险/成本元数据，以及四模式的轮次、工具次数和耗时预算。
- Agent 运行账本与任务中心执行回执：展示真实步骤、状态/文件证据和预算消耗。
- 专业两部分拉片协议：报告固定为“视频讲了什么”和“专业视听拆解与 AI 复刻”，关键帧覆盖全片并经过质量门禁。
- Preview、Beta、Stable 三条发布通道，以及标准安装器、便携 ZIP、发布清单、SHA-256、SBOM、安全扫描和可审阅 PowerShell 安装脚本的统一资产合同。
- 自然语言专业多轨音频：对白、音乐、环境声和音效可增删、对齐、分段调音量并自动闪避，最终成片逐轨做 PCM 时间对齐、响度与可撤销项目验证。
- 自然语言音频修复：本机完成降噪、去直流、EBU R128 响度匹配和短数字断音填充；立体声可另存基础人声/伴奏干线，并明确提示中侧声道分离伪影与“不恢复丢失对白”的边界。
- 自然语言节拍剪辑：从本地音乐解码PCM识别真实BPM、起音网格与高潮，用“更快/更克制”调整每次切镜跨越的拍数；执行前先给方案，成片验证切点画面变化、高潮密度、音乐相关和片尾画面/声音双淡出。

### 变更

- 字幕入口根据源语言自动反向输出：英文媒体显示中文，中文媒体显示英文；翻译字幕采用紧凑双行排版，不再铺满画面。
- 工具执行与模型文字分离：渲染器实际应用播放器动作后回读状态，成果文件由主进程检查授权路径、存在性和大小。
- Colibri 工具模式接回通用 OpenAI 兼容工具循环；无工具时继续使用其流式适配器。
- 桌面品牌名称、图标、安装包和快捷方式统一为 AgentPlay，同时保留内部 `ai-player` 数据身份以兼容历史设置。
- 数字签名不再阻塞明确标记为 GitHub Prerelease 的 Preview/Beta；Stable 仍要求安装包与主程序的 Authenticode、证书身份和时间戳全部有效。

### 修复

- 修复下载后自动播放、字幕生成/在线字幕/云模型入口之间提示混乱和任务断链的问题。
- 修复非 16:9 视频比例、播放器控制层唤醒、字幕位置和长文本排版问题。
- 修复任务执行时切换历史结果可能把进度写到错误任务的问题，并为旧持久化任务增加安全迁移。
- 修复模型宣称调用工具但渲染器未执行时仍被视为完成的问题。
- 修复全新 Windows 环境双击 MP4 报“没有注册类”的问题：增加不抢占现有默认播放器的 ProgID、OpenWithProgids 和默认应用能力注册，并在卸载时完整清理。
- 修复发布校验脚本依赖未声明的 7zip 路径，以及 Windows PowerShell 5.1 读取无 BOM 中文安装脚本时的解析失败。
- 修复重复文件扫描哈希在 Windows 文件句柄真正关闭前返回，导致结果正确但临时目录清理偶发 `ENOTEMPTY` 的竞态。
- 修复 WiFi 上传服务关闭后立即返回、Windows 测试和调用方无法等待 HTTP 连接真正释放的问题；`stop()` 现在返回可等待的关闭 Promise。

### 已知限制

- Windows 安装包尚未接入 Authenticode 签名，SmartScreen 可能显示“未知发布者”；请从官方发布页下载并核对 SHA-256。
- 云端翻译、深度拉片和内容生成需要用户配置对应模型；本地 AI 组件由应用内按需下载，不内置于标准安装包。
- Facebook 等需要登录态的平台可能要求用户登录或导入自己的 Cookies；没有有效登录态时不会伪报下载成功。
- macOS、Linux、Android 与 iOS 尚未完成与 Windows 同等级的端到端验收。

## [0.7.6] - 2026-08-10

### 新增

- X（Twitter）与 Facebook 链接接入统一的站点视频下载链。
- 链接识别后同时提供“仅下载”和“下载并拉片”，保留下载后的分析入口。
- 新增打包应用内的真实下载冒烟测试，以及下载/拉片选择策略回归测试。
- 新增基于 JSZip/Open XML 的确定性 PPTX 生成器与真实 PowerPoint 打开验收。

### 变更

- 更新 Electron、Vite、Capacitor、文档处理和安全相关依赖；生产依赖与完整依赖分别审计。
- 本地 AI 组件继续使用应用内按需下载，不进入标准安装包和 Git 历史。
- Office COM 清理只作用于本次任务新建的 Word、Excel、PowerPoint 进程，避免影响用户已有会话。
- electron-builder 子进程超时采用受测补丁，避免长时间构建被默认短超时误杀。

### 修复

- 修复发送视频链接后只出现“仅下载”、原“下载并拉片”入口丢失的问题。
- 修复 X 链接规范化、站点识别、登录态传递和下载进度处理。
- 修复 Facebook 链接进入错误通道或在缺少有效 Cookies 时给出不准确成功提示的问题。
- 修复下载取消、进度清理及站点登录状态回收边界。
- 移除 PPTX 生成链中的冗余依赖，降低供应链与打包面。

### 已知限制

- Facebook 的真实媒体下载通常需要用户自己的有效 Cookies；没有有效登录态时应明确失败，不能把解析到页面当作下载成功。
- Windows 安装包目前未接入公开代码签名，发布说明必须保留 SmartScreen 与 SHA-256 校验提示。
- macOS、Linux、Android 与 iOS 尚未完成与 Windows 同等级的端到端验收。
- Git 不包含模型、媒体样例和大型可再分发二进制；安装包可复现性还取决于外部资源清单、来源、许可证与哈希闭环。

## [0.7.5] - 2026-08-05

- 上一个公开稳定版。历史发布资产与说明见 GitHub Release。
