# AgentPlay 后续工作清单

## 2026-09-02 核心播放热修复（优先于推广）

- [ ] 通用本地音视频统一留在原播放区域（本地实现 `f597ddc`，公开修复 [PR #42](https://github.com/wg5759/AgentPlay/pull/42)）：编码预检、原片不变的播放缓存、全屏同节点与提前单实例退出已实现；用户已明确豁免本轮异源复审并授权安装/发布。桌面归档已同步；修复依赖告警后的 `54f13a0` 已通过双平台 CI。最终安装包、安装位矩阵和 Preview 3 公开哈希回读进行中，闭环前不勾选。详见 [验收与边界](docs/INLINE_PLAYBACK_HOTFIX_20260902.md)。

更新时间：2026-08-25。本文只记录尚未达到“稳定交付”标准的工作；代码入口或按钮存在不等于完成。

## 版本总方案与完成率

### 进度规则

- `[x]` 只表示该项已经同时完成实现、自动化回归、适用的真实安装态验收和证据留存；只有代码、按钮、计划或模型口头判断时不得打勾。
- `[ ]` 表示尚未达到稳定交付；可以已有可复用底座，但不计入该版本的功能交付完成率。
- 每个版本按下列 25 个验收项计算完成率，完成一个勾选项增加 4%；范围变化时先修改总方案和分母，不能靠删除未完成项提高完成率。
- `0.8.0` 进入功能冻结：只接受安全问题、数据损坏、崩溃、核心功能回归和发布阻断修复；所有新增能力进入 `0.9.x`。
- 用户只需描述新功能或期望结果；版本归类、执行规范、风险边界、测试、安装态验收和发布门由 Agent 维护。

### 0.8.0 冻结与发布收尾

功能开发完成率：**100%（已冻结）**。稳定公开发布闭环：**50%（3/6）**。

- [x] 0.8.0 功能范围冻结，源码合入公开 `master`。
- [x] 本地安装候选完成播放器、下载、文档、模型路由与持久任务验收。
- [x] 公开源码、社区入口和 Ubuntu/Windows CI 完成。
- [ ] 获得可核对的非维护者使用、反馈或贡献证据。
- [ ] SignPath 或等价开源代码签名计划正式获批。
- [ ] 安装包与安装后 EXE 签名有效，并以 `v0.8.0` Release 完成匿名下载、签名、哈希和 SBOM 回读。

未完成的后三项不阻碍 `0.9.x` 在独立分支开发，也不阻碍明确标为 GitHub Prerelease 的未签名 Preview/Beta 持续迭代；但不得把未签名候选写成稳定公开版。Preview/Beta 必须同时提供安装器、便携 ZIP、发布清单、SHA-256、SBOM、安全扫描和真实签名状态；Stable 仍要求安装包与主程序 Authenticode 均为 `Valid`。

状态核验（2026-08-21）：0.8.0 功能代码已通过 PR #16 合入 `master`，Ubuntu/Windows CI 成功，本机稳定安装程序版本为 0.8.0；但安装 EXE 仍为 `NotSigned`，GitHub 最新公开产品 Release 仍是 `v0.7.6`。因此“0.8.0 功能开发完成”与“0.8.0 签名公开发布完成”必须分开表述。

发布通道核验（2026-08-25）：已把 Preview/Beta 与 Stable 从规则层、资产生成和自动测试三处拆开。真实 0.8.0 本地 Preview 候选生成安装器与便携 ZIP，SHA-256 分别为 `660AA878...C2443F2` 与 `8C1CEB03...57E5546`；发布清单如实记录安装器和主程序均为 `NotSigned`，同一候选进入 Stable 通道会故障关闭。PowerShell 安装脚本在 Windows PowerShell 5.1 下通过 UTF-8 BOM 与“预发布必须指定精确版本”验收；全仓 `pnpm check` 为 898 tests、898 pass、0 fail、0 skip。该证据是本地候选闭环，不代表已创建 GitHub Prerelease、公开上传资产或完成签名。

### 0.9.0：万能内容入口与自然语言影音编辑 v1

严格交付闭环：**100%（25/25）**。实质代码覆盖：**100%（25/25）**。可复用底座准备度：**100%（5/5，不计入上述 25 项）**。

口径说明：`严格交付闭环` 只统计实现、自动回归、适用安装态验收和证据均齐全的 `[x]`；`实质代码覆盖` 还包含已存在主要代码但缺统一协议、全范围验收或最后收口的“已实现待收口”。“未实现”只用于当前代码中没有对应产品闭环的项目，不能把待收口误写成从零未开发。

审计基线：`3fa4b5b`。该分支相对 `master` 有 49 个提交、115 个文件发生变化、14843 行新增；全仓共有 135 个自动测试文件、43 个安装态 smoke 脚本。最近一次正式 `pnpm check` 为 784 tests、784 pass、0 fail、0 skip；提交后安全扫描 current 423、history 1670 无发现。E5 功能与安装态验收基线 `3fa4b5b` 已进入 Draft PR #22；Windows CI 发现的 DLNA 临时目录清理竞态由 `ce97311` 收口，GitHub Actions 32613899837 的 Ubuntu/Windows 与两类 npm audit 全部成功。以下25项状态来自代码、自动测试、真实媒体和安装态脚本逐项对照，不来自旧勾选数或主观估算。

已具备并必须复用的底座：

- [x] 统一文件/链接入口、可见预览和授权附件。
- [x] FFmpeg/mpv 媒体处理、字幕、配音、混音和 MP4 渲染能力。
- [x] 主进程持久任务、检查点恢复和不可覆盖原文件规则。
- [x] 云端、付费、发布、删除和凭证五类统一审批。
- [x] 成果文件、容器、字幕和语义质量评分与失败闭环。

#### A. 项目胶囊与持续记忆（5/5 闭环，5/5 有代码）

- [x] 一个项目可持续容纳视频、音频、网页、PDF、Office、图片、字幕和用户说明，不因重启丢失上下文。
- [x] 自动生成可审计的素材清单、版本、来源、内容指纹和派生成果关系，不重复处理相同内容。
- [x] 用户可在同一对话中继续修改上一轮结果，Agent 明确知道当前版本和修改对象。
- [x] 项目支持归档、复制和安全删除；删除进入可恢复区并遵守统一审批。
- [x] 用真实混合项目完成冷启动恢复、迁移、损坏恢复和大项目压力验收。

#### B. 任意文件与链接的统一理解（5/5 闭环，5/5 有代码）

- [x] 粘贴或拖入视频、音频、网页、公开 PDF、GitHub、在线文档和 RSS 后自动识别类型并先给可见预览。
- [x] 合法公开内容可阅读、下载、翻译、提取或加入项目；不绕登录、付费墙、版权与站点访问控制。
- [x] 视频时间点、文档页码、网页段落、表格单元格和图片区域形成统一证据引用。
- [x] 跨素材问答必须返回来源定位，无法从材料确认时明确标记推断或未知。
- [x] 使用公开/合成样本完成准确率、重复、超长内容、断网和登录态失效验收。

#### C. 按成果自动编排工作流（5/5 闭环，5/5 有代码）

- [x] 用户只描述最终成果，Agent 自动组合 OCR、字幕、翻译、拉片、文档、表格、演示稿和媒体工具。
- [x] 执行前只展示必要的成果、成本、上云和不可逆边界，不把内部工具链暴露成工程面板。
- [x] 一次任务可交付相互一致的 Word、PPT、PDF、表格、字幕或媒体成果包。
- [x] 每个成果带来源、质量评分、失败原因和可继续修改入口，不把生成文件等同于交付成功。
- [x] 至少用合同、研究资料和视频内容包三类真实工作流完成端到端验收。

#### D. 自然语言影音编辑 Agent v1（5/5 闭环，5/5 有代码）

- [x] 将文字或语音要求编译成带素材、时间码、轨道、操作、输出和质量条件的 `EditDecisionList v1`，歧义时只追问真正影响结果的一项。
- [x] 支持帧级精确截取、删除、拼接和顺序调整；例如“保留第 4 秒到第 20 秒”直接另存成约 16 秒成片并回读真实时长。
- [x] 支持用户本地音乐或合法公版/授权录音的添加、裁切、循环、音量、淡入淡出、对白闪避和响度归一；不得擅自下载商业录音。
- [x] 支持字幕生成、校对、删除、时间移动、反向翻译、样式调整，以及外挂字幕/烧录字幕两种输出。
- [x] 本地低风险且语义明确的编辑可直接执行；执行后给轻量时间线回执、预览、撤销和“继续用一句话修改”，始终不覆盖原文件。

2026-08-13 首个纵向切片（尚不足以勾选整项）：明确指令“保留第 4 秒到第 20 秒”已编译为冻结的 `EditDecisionList v1` 并进入统一工具注册表、持久任务、取消/恢复和质量评分；真实 9:16 MP4 另存成 16.023 秒新视频，ffprobe 回读 720×1280、H.264/AAC、源文件指纹未变；询问、否定、举例和无效时间段不会误执行。候选包对话流已完成 100 分质量验收并自动预览成片。

2026-08-14 第二个纵向切片（仍不足以勾选整项）：每次成功裁剪会先落盘非破坏性编辑项目胶囊，再收口持久任务检查点；用户直接说“撤销刚才的剪辑”可回到原片，说“重做刚才撤销的剪辑”可回到成片，所有版本文件均保留。项目历史跨应用重启恢复；历史损坏、目标版本被外部改写时故障关闭；从旧版产生新编辑会替换已放弃的重做分支。真实候选应用对话验收完成“16.023 秒成片 → 撤销回 222.284626 秒原片 → 重做回 16.02322 秒成片”，质量分 100。删除/拼接/重排、歧义追问以及项目级多操作继续编辑仍未完成，因此本组保持 0/5、版本完成率保持 0%。

2026-08-14 第三个纵向切片（仍不足以勾选整项）：明确指令“删除第 4 秒到第 8 秒”已进入同一 `EditDecisionList v1`、工作模型工具注册表、主进程持久任务、质量修复和非破坏性项目历史。FFmpeg 分别裁掉音画区间并拼接前后时间线；无音轨视频走安全的纯视频路径，删除后不复制已经错位的旧章节。真实候选应用先从 222.284626 秒原片保留出 16.023 秒版本，再以一句话继续删除成 12.068 秒第三版本；两个任务质量分均为 100，撤销/重做可在 16.02322 与 12.068209 秒版本间切换，三份文件均保留。质量自动修复只允许显式刷新同一任务版本的成果指纹，普通重复执行遇到外部改写仍故障关闭。拼接多素材、顺序重排、真正影响结果的歧义追问、音乐和字幕编辑尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-14 第四个纵向切片（仍不足以勾选整项）：同一当前视频内的两个或更多明确时间段可按用户口述顺序拼接和重排；“把第 8 秒到第 12 秒放前面，再接第 0 秒到第 4 秒”会冻结为两段连续目标时间线，不会自行按源时间排序，也不会把只说一个区间或带询问、否定、举例的话误执行。安装态候选应用在前三版基础上生成 8.023 秒第四版，质量分 100，可撤销回 12.068209 秒删除版、重做到 8.02322 秒重排版；首尾抽帧对照分别更接近指定源段（0.191/0.177）而非错误顺序（7.977/7.968），原片和前三版指纹均未变化。恢复时会重新校验每段源范围、目标连续性、总时长和完整时间线回执。跨多个独立素材拼接、帧边界级证明、真正影响结果的歧义追问、音乐和字幕编辑尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-14 第五个纵向切片（仍不足以勾选整项）：缺开始时间、缺结束时间、缺保留/删除操作、完全缺时间范围，或给出多个保留区间但未明确是否拼接时，Agent 只追问当前唯一影响结果的一项；用户下一句可短答、改成完整新指令、反转两段顺序或说“算了”。追问阶段不创建持久任务、不调用 FFmpeg、不修改文件；补齐后由主进程重新生成完整冻结 EDL，再进入既有持久执行与质量门。待补充内容只保存在主进程 5 分钟内存状态，界面仅持有随机恢复令牌；伪造、超时、跨视频复用均故障关闭，切换视频会取消旧追问。没有打开本地视频却要求剪辑时，界面只要求先打开或拖入素材。跨多个独立素材拼接、轨道级 EDL、帧边界级证明、音乐和字幕编辑尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第六个纵向切片（仍不足以勾选整项）：明确指令"给视频加背景音乐 <本地音频路径>"与"背景音乐音量调到 N%"已编译为冻结的 `media.add-music` 决策并进入主进程持久任务、非破坏性项目历史与质量修复清单；用户本地或合法音乐混流时默认音量 15%、淡入淡出、对白闪避（sidechain）与短音乐自动循环，缺文件时只追问唯一一项且带版权红线（不提供文件不抓网）。FFmpeg 实测：混音成果时长=源视频（±0.2s）、音轨在位、源文件指纹未变，覆盖已存在成果、音乐文件缺失、非音频格式均故障关闭。排障记录：filtergraph 中 `apad=whole_dur` 会让整条管线 frame=0 停摆（改由 amix duration=first 与输出 -t 收边），Windows 原生反斜杠路径必须进音频路径正则。跨多个独立素材拼接、轨道级 EDL、帧边界级证明、字幕编辑尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第七个纵向切片（仍不足以勾选整项）：明确指令"把这个视频和 <另一个本地视频路径> 拼起来"已编译为冻结的 `media.concat-sources` 决策并进入主进程持久任务、非破坏性项目历史与质量核查清单；2–20 个本地素材按给定顺序拼接，统一等比缩放加黑边居中到第一个素材的分辨率，无音轨素材补等长静音轨，成果时长必须等于各素材时长之和（±0.25s）否则拒绝交付。只说"把两个视频拼起来"时只追问第二素材这唯一一项，给出路径即由追问收口执行，询问句不误执行；逐素材授权校验、指纹快照与顺序一致性校验齐备，任一源变化即故障关闭。安装态候选应用对话验收：640×360 有声 4 秒主视频与 1280×720 有声 3 秒素材合并出 7.057 秒、640×360、音轨在位的新视频，质量分 100，逐素材时间线回执与可撤销项目胶囊（第 2/2 版）齐备，双源指纹未变，覆盖已存在成果故障关闭。轨道级 EDL、帧边界级证明、字幕编辑尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第八个纵向切片（仍不足以勾选整项）：明确指令"把字幕 <本地 .srt/.vtt/.ass/.ssa 路径> 烧进视频"已编译为冻结的 `media.burn-subtitles` 决策并进入主进程持久任务、非破坏性项目历史与质量核查清单；字幕逐条烧录进画面（ffmpeg subtitles 经 avformat 读文件，中文路径与中文字形均实测正常），成果时长必须等于源视频时长（±0.2s）否则拒绝交付，字幕文件超 20MB 故障关闭。只说"把字幕烧进视频"时只追问字幕文件这唯一一项，给出路径即收口执行，询问句不误执行；源视频与字幕文件指纹均未变。安装态候选应用对话验收：4 秒有声视频烧录中文 SRT 出 4.040 秒新视频，质量分 100，抽帧验证字幕活跃时刻与源同刻帧差异 1.227 显著大于安静时刻 0.100，烧录帧中文字形清晰可读，时间线回执与可撤销项目胶囊（第 2/2 版）齐备。字幕生成校对、时间移动、反向翻译、样式调整、外挂输出、轨道级 EDL、帧边界级证明尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第九个纵向切片（仍不足以勾选整项）：明确指令"把字幕 <本地 .srt 路径> 提前/延后 N 秒"已编译为冻结的 `media.shift-subtitles` 决策并进入主进程持久任务、非破坏性项目历史与专用质量核查（条目回执+字幕结构+可撤销胶囊）；提前=时间轴减 N（出现更早）、延后=加 N（出现更晚），完全移出 0 点之前的条目丢弃并计入回执，存活条目钳位到 0 起点并重新编号，GBK 编码源自动解码、成果统一 UTF-8 CRLF；交付前重新解析成果逐条核对时间与文本，与冻结决策不一致即拒绝交付。缺字幕文件只追问文件、缺秒数只追问秒数（原句两项都缺时先文件后秒数逐项收口），询问句不误执行；本切片只收 .srt，ass/vtt 明确拒绝。配套修正：字幕类成果的编辑项目锚在字幕文件而非当前视频，"撤销刚才的剪辑"在当前文件无编辑历史时回退到最近更新的项目（无任何项目仍按旧报错故障关闭）；.srt 等非视频成果只给回执不再自动送进播放器。安装态候选应用对话验收：2 条字幕提前 2 秒产出 [0–0.5s（钳位）、1.0–1.8s] 新 srt，逐条毫秒级一致，质量分 100，源字幕与视频指纹均未变，播放器始终停留在原视频，撤销回到第 1/2 版。字幕生成校对、反向翻译、样式调整、ass/vtt 调时与外挂输出、轨道级 EDL、帧边界级证明尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第十个纵向切片（仍不足以勾选整项）：明确指令"把字幕 <本地 .srt/.vtt/.ass/.ssa 路径> 封装进视频"已编译为冻结的 `media.mux-subtitles` 决策并进入主进程持久任务、非破坏性项目历史与质量核查清单；字幕作为可开关的 mov_text 独立轨道封进 mp4，音画流直接 copy 不重编码（实测秒级完成），成果时长必须等于源视频时长（±0.2s）且必须真实带字幕轨（ffprobe 复核），否则拒绝交付。只说"把字幕封装进视频"时只追问字幕文件这唯一一项，给出路径即收口执行；烧录动词与封装动词严格分流（"烧进/烧录/硬字幕"仍走逐条烧录），询问句不误执行。安装态候选应用对话验收：4 秒有声视频封装中文 SRT 出 4.000 秒新视频，mov_text 字幕轨在位、音画仍 h264/aac 未重编码，质量分 100，源视频与字幕文件指纹均未变，时间线回执与可撤销项目胶囊（第 2/2 版）齐备；外挂与烧录两种字幕输出至此都可用。字幕生成校对、反向翻译、样式调整、ass/vtt 调时、轨道级 EDL、帧边界级证明尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第十一个纵向切片（仍不足以勾选整项）：明确指令"把字幕 <本地 .srt 路径> 翻译成英文/中文/双语"已编译为冻结的 `media.translate-subtitles` 决策并进入主进程持久任务、非破坏性项目历史与专用质量核查（条目回执+目标语言检测+可撤销胶囊）；双语目标不给语言时自动判源语言对面（中文源→英文、英文源→中文）。引擎在入队前冻结：云端已配置时先过原生同意框（拒绝则英译中回退本地 OPUS-MT 离线组件），恢复时按冻结路由重建、路由变化或凭证失效即故障关闭；任一批次翻译失败即拒绝交付半成品，交付闸门复核条数、时间轴（双语逐条相等）与目标语言真实性。范围切割：不给 .srt 路径不接管（"翻译字幕"仍走既有视频级双语生成流程），缺目标语言只追问这一项，询问句不误执行。安装态候选应用对话验收（全新 profile 无云配置，自动走本地离线引擎）：2 条英文字幕追问收口后译成中文（"Hello, welcome to this video."→"欢迎收看这段录像"），时间轴逐条一致，质量分 100，源字幕与视频指纹均未变，播放器始终停留在原视频。字幕生成校对、样式调整、ass/vtt 调时与封装、轨道级 EDL、帧边界级证明尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第十二个纵向切片（仍不足以勾选整项）：烧录字幕支持一句话指定样式——"字号大一点/小一点"（FontSize 32/16）、"放顶部/底部"、"黄色/白色/红色/蓝色/绿色/黑色"，编译进冻结的 `media.burn-subtitles` 决策（subtitle.style）并经 `force_style` 落到 ffmpeg 烧录管线，回执与成果名后缀如实标注样式。排障记录：force_style 的 Alignment 走 SSA v4 语义（6=顶中、2=底中，不是 ASS 小键盘的 8）；YUV 有限范围会把亮黄 254 压到 147，像素级验证谓词必须用通道差而非绝对亮度；一条输出流只认一个 -vf（后写覆盖前写，字幕滤镜会被静默丢弃）。安装态候选应用对话验收："烧进视频，字号大一点，放顶部，黄色"产出 4.040 秒新视频，抽帧核验顶部带黄色字幕像素 48、底部带 0，帧图人眼复核黄色大字顶部居中、中文字形清晰，质量分 100，源视频与字幕文件指纹均未变，可撤销项目胶囊（第 2/2 版）齐备。字幕生成校对、ass/vtt 调时、轨道级 EDL、帧边界级证明尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第十三个纵向切片（仍不足以勾选整项）：字幕条目校对/删除——"把字幕 <本地 .srt> 第 3 条改成《新文本》"、"删掉第 2 到第 4 条"（含"第2到第4条/第2条到第4条/第三条"写法）编译为冻结的 `media.edit-subtitle-cues` 决策并进入主进程持久任务、非破坏性项目历史与专用质量核查（条目数期望由冻结决策直接推算）；删除后存活条目重新编号且时间轴不重排，越界、删空、区间改文本、覆盖已存在成果均故障关闭，交付前重解析逐条核对，与冻结决策不一致即拒付。追问链：明说"第 N 条字幕"但没给文件时追问文件（不再错误落到视频段"删除哪一段"追问）；有文件缺序号只问序号；改文本意图先序号后文本逐项收口；"第 N 秒"视频流与询问句不受影响。安装态候选应用对话验收双流连通：直接改错字（"今天天汽不错"→"今天天气不错"，时间轴不动）与"删掉第3条字幕"追问文件后删除（3 条变 2 条重编号），质量分均 100，源字幕与视频指纹均未变。ass/vtt 调时与校对、轨道级 EDL、帧边界级证明尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-18 第十四个纵向切片（仍不足以勾选整项）：字幕调时与条目校对扩展至 .vtt——WebVTT 两种合法时间写法（省略小时的 MM:SS.mmm 与完整 HH:MM:SS.mmm）与 cue 标识行均可解析，成果保留 WEBVTT 头、点号毫秒与 .vtt 扩展名；决策的 output.container 随源格式（srt/vtt）冻结，任务、质量核查与项目历史同构复用。安装态候选应用对话验收：2 条 VTT 字幕延后 2 秒逐条毫秒级一致（含 cue 标识行源），质量分 100，源字幕与视频指纹均未变。ass 调时与校对、轨道级 EDL、帧边界级证明尚未完成，因此本组仍保持 0/5、版本完成率仍为 0%。

2026-08-21 第十五个纵向切片（仍不足以勾选整项）：精确保留片段新增首尾帧边界证明。主进程从成片首帧/末帧与冻结 EDL 对应的源片候选帧读取 32×32 灰度样本，以平均绝对差和相邻时间对照余量生成 `gray-frame-mad-v1` 回执；明确错位在原子交付前拒绝，证明缺失或采样不可用失败关闭，画面完全相同导致无法唯一判定时如实降为 warning 且不冒充满分。帧读取进程排空错误流并有 60 秒硬超时，损坏媒体不会无限挂住任务。恢复已有成品时重新生成同一证明；该切片当时尚未覆盖删除、拼接、跨素材、配乐和字幕类操作，未实现的操作不显示伪“通过”检查。真实运动素材与安装态候选均须得到 `matched`、帧证明质量项通过且总分 100。删除与同源拼接的后续覆盖见第十六个纵向切片；跨素材与轨道级 EDL 仍未完成，因此本组保持 0/5、版本完成率保持 0%。

2026-08-21 第十六个纵向切片（仍不足以勾选整项）：`gray-frame-mad-v1` 扩展到删除片段和同源拼接/重排。删除会把所有实际保留段编译成连续目标时间线，拼接会沿冻结口述顺序逐段证明；每个片段的首帧与末帧都必须相对源片边界匹配，同总时长但错删 0.3 秒或顺序相反的成品会在恢复时失败关闭。证明缺失、采样不可用或明确错位进入质量硬失败，静态画面仍诚实 warning；质量卡只摘要前 4 段，其余证据保留在完整回执。FFmpeg 完成后与帧证明完成后各复核一次源文件大小和修改时间，防止证明期间源视频被外部改写。真实 24 秒运动素材得到 16 秒保留版、12 秒删除版和 8 秒重排版：删除 2 个保留段、重排 2 个片段的 8 个首尾边界全部 `matched`，源文件与历史版本指纹不变。最终交付门要求隔离候选对话中的删除/拼接任务也携带证明、质量 100、回执文案可见并保持撤销/重做。跨素材拼接、配乐和字幕操作本轮不显示帧证明；帧无损剪切与轨道级 EDL 仍未完成，因此本组保持 0/5、版本完成率保持 0%。

2026-08-21 第十七个纵向切片（仍不足以勾选整项）：逐段帧证明扩展到跨素材拼接。每个源素材先按成片目标画布执行与拼接管线相同的等比缩放、居中黑边、SAR 归一化，再缩为 32×32 灰度证据；不能把未归一化的小分辨率源帧直接与带黑边成片比较。素材数、逐素材时间线回执数和证明段数必须一致，缺失、不可用、不完整、错位均进入硬失败；恢复时重新探测每个素材的时长和分辨率，B→A 不能冒充 A→B。真实测试覆盖 A=640×360/15fps/有声 4 秒、B=320×240/24fps/无声 3 秒且不同色相，成片统一为 640×360、有音轨、约 7 秒，两个素材的 4 个首尾边界全部 `matched`，第二素材自动补静音，两个源文件指纹不变。隔离候选安装态先验确认夹具分辨率/帧率不同且 B 无音轨；用户对话经缺素材单问收口后，任务用时 8.356 秒，帧证明 2 段、质量 100、逐素材回执和自动预览均通过，两个源文件未改动。配乐和字幕编辑仍不显示帧证明；帧无损剪切与轨道级 EDL 尚未完成，因此本组保持 0/5、版本完成率保持 0%。

2026-08-21 第十八个纵向切片（当时仍不足以勾选整项）：配乐从“音轨存在”升级为 `decoded-pcm-s16le-v1` 声音质量证明。主进程扫描整段首音轨的解码样本均值与样本峰值，并在 5 个内容窗口将成片与原声 PCM 对齐后计算最小二乘残差；没有可确认变化、静音、证明缺失或样本峰值达到安全上限均在原子交付前失败关闭。淡入淡出使用已解码音乐源窗口与同一条已执行 FFmpeg 包络参数核对，成片边缘窗口残差同时留在回执；对白闪避只声明 sidechain 配置事实，不冒充独立分轨或专业母带测量，样本峰值也不冒充 true peak/LUFS。混音尾端新增不自动增益的 limiter；程序重启发现已有成果时按源片时长重新生成整套声音证明，不再错误套用普通裁剪的时间线字段。真实测试覆盖有原声、无原声和已有成果恢复；隔离候选安装态一句话配乐用时 8.070 秒，成片 4.02322 秒、质量 100、5/5 内容窗口确认变化、样本峰值 -17.4 dBFS、淡入/淡出包络比 0.12/0.08，源视频与音乐文件指纹均未变化。候选 `AgentPlay.exe` 仍为 `NotSigned`；音乐选段与真正响度归一随后在第十九切片补齐。

2026-08-21 第十九个纵向切片（完成配乐项）：自然语言配乐决策新增音乐 `selection`、循环/只播放一次策略和最终总线 `loudness`；默认采用 -16 LUFS、TP ceiling -1.5 dBTP、LRA 11，两遍 loudnorm 第一遍测量、第二遍带 measured 参数执行，AAC 编码后再用独立 `ebur128=peak=true` 复测，质量门要求 LUFS 偏差不超过 0.7 且 true peak 不高于 -1 dBTP。音乐选段先原子写为 48k PCM，再只循环该片段；“只播放一次”会在音乐结束后保留原声或补有限静音到完整视频时长。真实三频段夹具 220Hz→880Hz→330Hz 中只选 2–4 秒，成片 0.8 秒与 3.2 秒均实测约 880Hz；单次播放夹具在音乐结束后为数字静音，越界选段拒绝。视频与音乐同时进入主进程持久指纹快照，FFmpeg 运行或声音证明期间任一文件变化均在原子交付前拒绝。最终隔离候选安装态执行“音乐 1–3 秒循环铺满、-16 LUFS”用时 7.528 秒，成片 4.02322 秒、质量 100、编码后 -16.0 LUFS / -11.6 dBTP，应用回执与独立 EBU R128 复测一致，双源指纹未变化；完整 `pnpm check` 为 719 tests、716 pass、0 fail、3 个外网条件 skip，安全扫描 current 390、history 1496、packaged 5 均无发现。候选 EXE/ASAR 哈希分别为 `66FF07374890343BD56A0C0F74AB47B7287C0347A7D0FA57AD91549C7546168E` / `AF0E88D6769B7A83C8FD728C8AA8FA1B5D79610076CC88CDC82EDAB7DC1A1C5F`，EXE 仍为 `NotSigned`，不冒充稳定发布版。

2026-08-21 第二十个纵向切片（完成自然语言影音编辑 D 组）：新增统一 `agentplay.edit-decision-list` 协议，把现有 10 类裁剪、删除、同源/跨素材拼接、配乐、字幕烧录/封装/调时/翻译/条目编辑决策全部规范成 `materials + tracks + operations + output + quality`。明确计划和追问收口计划都会附加 EDL；主进程入队冻结整份协议，10 个持久编辑执行器在恢复或执行前重建 EDL 并逐字段比较，时间码、素材、轨道、输出或质量条件任一被篡改都以“EDL 与冻结决策不一致”故障关闭。TypeScript 桥接公开同一 schema，工作模型和确定性入口共享主进程权威合同。单元/合同测试新增 9 项，全部媒体相邻回归 63/63；正式 `pnpm check` 为 729 tests、726 pass、0 fail、3 个外网条件 skip，安全扫描 current 392、history 1510、packaged 5 无发现。隔离候选以真实长片完成明确计划、追问收口、持久任务 spec 三处 EDL 一致性验收；16.023 秒裁剪、12.068 秒删除、8.023 秒重排质量均为 100，帧证明全部 matched，四版本撤销/重做通过。候选 EXE/ASAR SHA-256 为 `D9EC966082D76681807D768507E6B43131474FCDA82D77EBC7EA63CD0EDFF0B3` / `E15DDABB36720BA0DC2961D284E7034C5BBCA87B506A35DE16FFC4057B72DC2F`，EXE 仍为 `NotSigned`。至此 D 组 5/5 严格闭环，0.9.0 固定分母严格闭环为 7/25=28%、实质代码覆盖仍为 19/25=76%，待收口项降为 12。

2026-08-22 第二十一个纵向切片（完成 C3/C4）：成套文档不再让不同格式各自自由取材；主进程先从已授权来源生成带 `F1...Fn` 编号与 SHA-256 的冻结事实底稿，DOCX/XLSX/PPTX/PDF/MD/TXT 每次格式生成必须声明所用事实编号，未知编号或未绑定底稿即按该格式失败。写出后生成 `agentplay.delivery-receipt`，逐一记录来源、文件或目录成果、字节数、内容哈希、请求/完成/失败格式和共享底稿；质量门重新计算成果指纹，部分格式、成果被改写、来源缺失或底稿不一致均失败关闭，已完成文件和稳定失败原因仍返回用户。任务中心直接任务与重启恢复路径统一显示质量评分、精简来源/一致性证据和“继续修改”，点击后文档成果重新附回入口、媒体成果重新打开。正式 `pnpm check` 为 732 tests、728 pass、0 fail、4 个外网条件 skip。最终隔离候选以真实 TXT 经本机受控 OpenAI 兼容服务一次生成 DOCX+XLSX，两次格式调用均携带同一冻结底稿；交付回执 `complete`、一致性 `matched`、质量 100，来源、质量、一致性和继续修改四处 UI 均通过。候选 EXE/ASAR SHA-256 为 `05D4CFEFCDDE61DE850B83AA8D7BDDC7405901DE79196D42BB9612001475AFA9` / `2C5F9E81CD5A9D30C0EBF21FEE39AC533C24B38E335E0A061098879C0010CD2A`，EXE 仍为 `NotSigned`，稳定安装未覆盖。至此 C 组 3/5 严格闭环，0.9.0 固定分母严格闭环为 9/25=36%、实质代码覆盖仍为 19/25=76%，待收口项降为 10。

音乐版权特别说明：古典作品的曲谱可能已经进入公版，但某个现代演奏录音仍可能受录音版权保护。用户说“加《月光奏鸣曲》”而没有提供音频时，Agent 只提供“选择本地合法文件”或“从已接入的公版/授权曲库选择版本”，不能从未知网站自动抓取。

#### E. 后台完成与主动交付（5/5 闭环，5/5 有代码）

- [x] 任务进入后台后用户可关闭工作区，应用重启继续且不重复付费调用。
- [x] Windows 通知展示真实完成、待审批或失败状态，点击回到原对话和对应结果。
- [x] 长任务给阶段和合理时间范围，不显示假进度或要求用户停留页面。
- [x] 失败只给稳定原因和一个明确修复动作；本地安全任务可按质量策略自动修复一次。
- [x] 安装态完成任务中断、崩溃、重启、审批过期、通知点击和成果回开闭环验收。

2026-08-22 第二十二个纵向切片（完成 E1/E3）：文档单成果在模型结构化方案返回后立即落 `ai-plan-ready`，成套成果每完成一个格式即落 `bundle-section-complete`；恢复和本地质量修复复用已完成模型结果，只调用未完成格式。视频拉片在专业质量门完成后先持久化分析稿，重启只重新提取本地关键帧和确定性写报告；字幕翻译逐批持久化已完成条目，恢复只发送尚未翻译的条目；AI 视频与重构继续复用远端 `videoId`、镜头计划和逐镜任务检查点。顶部进度只保留真实处理阶段，不再把“继续创作/继续编辑/查看结果”点亮成已执行流程；真实百分比可解析时显示真实值，未知进度使用无数值往返光带，并按任务类型显示可解释时间范围。正式 `pnpm check` 为 737 tests、735 pass、0 fail、2 个外网条件 skip。最终隔离候选冷启动恢复文档任务 attempts 1→2、收口至 `history-written` 且成果内容核对通过；运行中文档只点亮“读取文件/理解需求”，未提前点亮“生成成果”，时间范围可见、无默认30%假值。候选 EXE/ASAR SHA-256 为 `F4AF7C3E25D2C34E6569C3C46EE0C76D6A3295B1A592429004C698F15C96DD16` / `38EE7AAE4D8C460567EED444F03ABBF0F51845E20849307941092748CD6CB134`，EXE 仍为 `NotSigned`，稳定安装未覆盖。至此 E 组 3/5 严格闭环，0.9.0 固定分母严格闭环为 11/25=44%、实质代码覆盖仍为 19/25=76%，待收口项降为 8。

2026-08-23 第三十个纵向切片（完成 E2）：主进程将持久任务的 `waiting_approval / completed / failed` 三种真实状态写入本地通知历史，并仅在系统支持且应用不在前台时调用 Windows 原生通知；同一任务状态按稳定键去重。完成通知只显示成果文件名，不泄露完整路径；Linux CI 发现 Windows 反斜杠路径在非 Windows 主机上无法由 `path.basename` 脱敏后，改为宿主无关的双分隔符解析，并补齐 Windows/POSIX 两类回归。点击通知会恢复、显示并聚焦主窗口；渲染器未就绪时激活事件排队，随后精确选择原工作区任务并重新打开原成果。正式 `pnpm check` 为 781 tests、781 pass、0 fail、0 skip，安全扫描 current 421、history 1666 无发现；GitHub Actions 32612210957 的 Ubuntu/Windows 质量门与两类 npm audit 全部成功。最终隔离候选真实生成待审批、完成、失败三类系统通知，完成文档质量100，点击回到 `workspace-notification-completed` 并回开同一 DOCX；EXE/ASAR SHA-256 为 `197E9841A2203ABDAB8EB8510713C4207204E3876803D0CA2EFC7BF5FF9B42DF` / `3CC185F72CF25B47C2F66976B8E6061099B00D26D379585C43F619AAC426C703`，EXE 仍为 `NotSigned`，未覆盖稳定安装。至此 E 组 4/5 严格闭环，0.9.0 固定分母为 24/25=96%，余下 E5 仍为明确未实现。

2026-08-23 第三十一个纵向切片（完成 E5）：补齐冷启动审批过期收口，待审批任务在令牌过期后重启不再永久停留在“等待确认”，而是自动标记 `failed / expired`、执行次数保持0并发出失败通知。最终隔离候选以真实本机模型执行 DOCX+XLSX 成果包，在 DOCX 已完成且 XLSX 调用仍在运行时强制终止主进程；同一用户数据目录冷启动后 attempts 1→2，从 `bundle-section-complete` 恢复到 `history-written`，已完成 DOCX 模型步骤未重调，两份成果真实落盘。过期云端审批没有执行；完成与失败两类 Windows 原生通知均真实显示，点击完成通知精确回到 `workspace-e5-crash-recovery` 并回开同一 DOCX。正式 `pnpm check` 为 784 tests、784 pass、0 fail、0 skip，安全扫描 current 423、history 1670 无发现；可复算回执保存于 `artifacts/acceptance/background-delivery-e5/receipt.json`。最终候选 EXE/ASAR SHA-256 为 `88173D6E1D8073F54C6F360B2E820C1C2024EF524A833D90152E9BCE70063A0A` / `F717C077FD9EDB6C07A8EFC80CA556DC679CE69292C56BDEF6E6F9449ECAC7FE`，EXE 仍为 `NotSigned`，未覆盖稳定安装。至此 E 组 5/5、0.9.0 固定分母 25/25=100% 严格闭环；签名和稳定公开发布仍属于 0.8.0 发布边界，不能混写为已完成。

2026-08-22 第二十三个纵向切片（完成 C1）：新增主进程 `agentplay.outcome-workflow`，把用户只描述最终视频成果的要求冻结为 `video.evidence-analysis → document.consistent-bundle`，后一步必须消费前一步真实落盘底稿；整体任务统一云端审批、来源指纹、步骤依赖、取消、检查点、恢复和质量门，第一步完成而第二步中断时只恢复最终打包，不重复拉片模型调用。扫描 PDF/图片的 OCR→多格式成果继续复用文档工作区既有自动提取链；视频链自动读取相邻字幕、反向翻译英文证据、使用关键帧/媒体分析并生成中文拉片底稿，再从同一底稿生成 Word/PPT/Excel/PDF/Markdown 中至少两种最终成果。任务中心统一显示原视频来源、两步成果回执、最终质量和继续修改。正式 `pnpm check` 为 742 tests、739 pass、0 fail、3 个外网条件 skip。最终隔离候选用带真实 SRT 的本地 MP4 和受控本机模型执行一句话“做成中文拉片报告、PPT 和 Excel 分析表”：模型调用严格为 analysis 一次、DOCX/XLSX/PPTX 各一次，三份成果可打开、质量100、共享底稿一致性 `matched`，任务中心五项可见验收通过。候选 EXE/ASAR SHA-256 为 `E3FBDC5C542B3B0F1EDC7A5158924DE43E60FF3CAA97E35B9AFAAF58B171891B` / `3F08CADB72F45D7D320E4A8B9568DCAFB38A2865E9759236EE3CF70EA72AB2B6`，EXE 仍为 `NotSigned`，稳定安装未覆盖。至此 C 组 4/5 严格闭环，0.9.0 固定分母严格闭环为 12/25=48%、实质代码覆盖仍为 19/25=76%，待收口项降为 7。

2026-08-22 第二十四个纵向切片（完成 A1/A2/A3）：新增主进程 `ProjectCapsule v1`，统一保存视频、音频、网页引用、PDF、Office、图片、字幕、用户指令、中间底稿和最终成果；按内容 SHA-256+字节数去重，同路径内容变化形成新素材版本，成果记录 `derivedFrom` 血缘，项目/素材/成果/指令/修订均有稳定 ID。任务准备阶段根据任一原素材或派生成果定位既有项目；相同来源+指令+输出的 operation key 命中且源/成果指纹未变时直接复用，不重新调用模型；成果被外部改写则拒绝复用。上一轮成果作为下一轮输入时不重复登记成原素材，而是把新成果指向上一版 artifact；任务中心显示项目第N版和当前修改对象，项目只读 API 可回读完整清单。正式 `pnpm check` 为 746 tests、746 pass、0 fail、0 skip。最终隔离候选先生成视频拉片DOCX/XLSX/PPTX，第二次相同请求模型调用保持4次不增加，再拿DOCX继续转TXT：同一项目从第1版升至第2版，最终清单为2份素材、5份成果、2条指令，质量100且UI通过。候选 EXE/ASAR SHA-256 为 `B7F61094453825B328F8C434518FE924E28D4B2D33590F32E9EB7539C4DF22F6` / `6768ECD3193484E5CF683906937A14CBD82690B9F44DC06B70A25A46B0FF26F3`，EXE仍为`NotSigned`，稳定安装未覆盖。至此A组3/5严格闭环，0.9.0固定分母严格闭环为15/25=60%、实质代码覆盖仍为19/25=76%，待收口项降为4。

2026-08-22 第二十五个纵向切片（完成B1/B2/B3/B5）：新增无凭证公开链接入口，优先于裸链接视频下载误判，统一识别普通网页、公开PDF、GitHub、RSS/Atom、在线文档、音频、媒体直链和视频站；公开网页返回标题、摘录和段落证据，可下载为本地附件、翻译或加入ProjectCapsule。所有请求只发送公开Accept/User-Agent，不发送Cookie、Authorization或浏览器登录态；私网/元数据地址、跳转到追踪/登录页、401/403/407/451、登录/订阅/付费墙均停止，不绕访问控制。新增`EvidenceReference v1`统一video-time、document-page、web-paragraph、sheet-cell、image-region；本地PDF/XLSX/图片/视频可回读页码、单元格、区域和字幕时间。固定120条公开/合成分类语料准确率100%，追踪参数/fragment去重，超2MB、断网和登录失效均有稳定失败。正式`pnpm check`为752 tests、750 pass、0 fail、2个外网skip。安装态正确识别GitHub/PDF/RSS，example.com得到3条段落证据并加入项目，本地XLSX/PDF/PNG/MP4分别返回sheet-cell/document-page/image-region/video-time。候选EXE/ASAR SHA-256为`8319AA21A04941F0542B43FD80416B591DC8D308DF1FC9DE4FACED4CAA70D15A`/`3EF4B50EB86ADEE96DA9A5E7684E8F132A1744A8C0F052C41A18AACBA05E1E50`，EXE仍为`NotSigned`。至此19项已有实质代码全部严格闭环，0.9.0固定分母严格闭环与实质代码覆盖均为19/25=76%，不存在“已实现待收口”；余下6项仍为明确未实现，不得自动冒充完成。

2026-08-22 第二十六个纵向切片（完成A4）：ProjectCapsule支持归档/取消归档、元数据级复制、统一delete审批、可恢复回收区和恢复。复制生成新project/material/artifact/instruction/revision ID并保留血缘与原文件位置，不复制大型素材；删除由`project.trash`持久任务冻结projectId，返回一次性delete审批对象和恢复令牌，未批准不改变项目。批准后只把项目胶囊移入trash，绝不删除任何素材或成果文件；恢复保留原项目ID、版本和血缘，不提供不可恢复purge入口。正式`pnpm check`753 tests、752 pass、0 fail、1外网skip。安装态完成归档→激活→复制→delete审批→回收→恢复，所有成果文件仍存在。候选EXE/ASAR为`4592DB85CEBD3C611E81467AF411CF81C123C106EBD1E3A51917C0543C55840F`/`9743E2A68FD19FA19D2003B02C17B6DF711729AA2552B57DC3AB163BF45B9F47`，NotSigned。固定分母升至20/25=80%，余下5项为未实现。

2026-08-22 第二十七个纵向切片（完成A5）：ProjectCapsule 每次持久化保留结构有效的上一状态备份；主 JSON 损坏时只允许从校验通过的 v1 备份恢复，并把损坏原件保留为 `corrupt-<timestamp>` 取证快照。v0 旧状态只迁移一次，改写前保留 `legacy-v0-<timestamp>`；未知更高 schema 拒绝用旧备份降级覆盖，无可信备份的损坏状态仍失败关闭。正式 `pnpm check` 为 757 tests、755 pass、0 fail、2 个外网条件 skip。隔离候选第一次冷启动把包含视频、音频、字幕、图片、PDF、Office 和文本的 v0 混合项目迁移为 v1，第二次在主状态人为损坏后恢复；201 个项目、2001 个修订均可回读，两次启动分别 3.581 秒和 3.206 秒，素材哈希未变。原有成果包、零调用复用、继续修改和归档/复制/回收/恢复安装态闭环再次通过，质量 100。候选 EXE/ASAR SHA-256 为 `FEC5C54A14B5B8AC7FABF73B883B16A7939F1808413E210DA5D2E1D601E7C3BD` / `DED868DE28F630CB1BF0825527203E542B684F44FE301B5873C25812CE4D6FAB`，EXE 仍为 `NotSigned`，未覆盖稳定安装。

2026-08-23 第二十八个纵向切片（完成B4）：新增 `project.evidence-qa` 主进程持久任务与 `ask_across_materials` 统一工具，在模型调用前冻结素材指纹、问题、模型路由和 EvidenceReference；重启时复用已提取证据，不改用其他模型。模型只能返回 `confirmed / inference / unknown` 三态主张；`confirmed` 必须引用存在的证据ID，`unknown` 禁止伪造引用，最终文案由确定性渲染器生成而不直接信任模型自由文本。云端引用不合格时立即失败关闭，不静默二次付费；本地模型最多修复一次。安装态验收先证明云端形态审批前0调用、摘要明确“不上传原文件”并可取消，再由受控本机模型一次调用完成核对：已确认结论同时定位 Excel `数据!B2` 与视频 `00:04–00:08`，二月数据标为“未知”，质量100、项目修订落盘、原文件哈希未变。候选 EXE/ASAR SHA-256 为 `436A01B646937D6FB8A5D7F61C916C5C8F0EEAF9B837D5C497768D38E38AD1B4` / `099AE4932723603871D0F8F3673EAD1D3819473D6F726982DEE02A302283C8B1`，仍为 `NotSigned`且未覆盖稳定安装。

2026-08-23 第二十九个纵向切片（完成C5）：新增 `agentplay.real-workflow-acceptance` 固定验收协议，拒绝微型假文本、无法回开成果、缺少冻结事实底稿、改写原件或只验收两类场景。合同使用用户曾实际打开的4,123字DOCX，包含阳台、租金、押金、违约与甲乙方条款，交付DOCX/XLSX/PDF；研究工作流使用3份真实模型接入与互审资料，交付DOCX/XLSX/PPTX；视频使用16.875秒、1088×832 H.264真实成片，提取12张关键帧并交付DOCX/XLSX/PPTX。三类质量均100，模型调用严格为3/3/4次，每个成果均真实落盘、回开、共用冻结底稿并进入原项目继续转TXT，继续修改未新增模型调用。合同与研究内容只发给受控loopback模型，云上传0；所有原件哈希未变。验收过程同时修复两个真实缺口：本地视觉模型现在可按冻结vision能力抽帧；模型路由将`127.0.0.1/localhost/::1`统一识别为本机，“只在本机”可选它，“云端”不会误选它。候选 EXE/ASAR SHA-256 为 `D860F056B39516FB7AAE0479038150695FFA82117EE48120283E7671547B7CF4` / `82FA95280396FC7161A461F50548760793EF5CE06FA12090B11985384F2C4C0D`，仍为`NotSigned`且未覆盖稳定安装。可复算回执保存在`artifacts/acceptance/real-workflows-c5/receipt.json`。

范围状态结论：固定分母中 25 项已形成统一协议、自动回归和适用安装态闭环；0.9.0 规划范围没有“待收口”或“未实现”尾巴。此处 100% 只表示本版本固定功能范围闭环，不代表未签名安装包已经成为稳定公开版，也不替代 0.9.1 的后续规划。

### 0.9.1：自然语言专业影音编辑 Agent

功能交付完成率：**100%（25/25）**。0.9.0 的 `EditDecisionList v1`、项目胶囊、预览/撤销和安装态质量门继续作为依赖。

#### A. 语义剪辑（5/5）

- [x] 根据转写与镜头证据执行“删掉废话、停顿、重复和跑题段落”，每个删除决定可定位、可恢复。
- [x] 支持“从他说到某句话开始”“保留讲产品价格的部分”等内容语义定位。
- [x] 自动检测静音、口头禅、重复镜头和明显失焦/黑帧，默认先给编辑方案再批量执行。
- [x] 支持长视频自动生成短版、精华版、章节版和多个平台时长版本。
- [x] 用访谈、课程、剧情、产品介绍和竖屏口播五类素材完成人工质量标定。

2026-08-23 首个纵向切片（仍不足以勾选整项）：明确指令“删掉超过 N 秒的长停顿”进入主进程 `audio-silencedetect-v1`，只扫描一次真实音轨并冻结检测区间、删除区间、呼吸边界和连续保留时间线；默认只处理片中超过 0.9 秒的停顿，允许用户指定 0.5–5 秒阈值，每处保留 0.12 秒自然呼吸，片头片尾静音不擅自删除。冻结方案作为 `EditDecisionList v1` 交给既有 `media.edit-concat` 持久任务，不由渲染器重算；执行继续复用原子另存、源指纹复核、帧边界证明、质量门、项目版本和对话撤销。帧证明同时修复一个真实误判：成片边界既匹配目标帧、又匹配相邻同画面时应为 `inconclusive`，不能因余量为0误报 `mismatch`。真实10秒音画夹具检测并删除 `2.12–3.28s` 与 `6.12–7.40s` 两处停顿，生成7.622秒成片，原片哈希不变。隔离候选从对话输入同一句话后自动预览新片、质量95通过，并可用“撤销刚才的剪辑”回到10.04秒原片；画面在静音区间内相同，帧证明诚实标记 `inconclusive`，不冒充唯一匹配。正式 `pnpm check` 为795 tests、795 pass、0 fail、0 skip，安全扫描current432/history1688无发现；候选EXE/ASAR SHA-256为`C49A3FA041E8A32734B8133F00B3F6777DFCE9E12CF88446A6EA20865C39B707`/`572672353666C1C7E8FEA5948AD25987CAA15B306BDAC873657354D551B6F131`，仍为`NotSigned`。废话、重复、跑题的语义证据与五类素材人工标定尚未完成，因此A1和0.9.1总完成率均保持0%。

2026-08-23 桌面发现问题修复：用户连续询问“你是谁/具体能完成什么任务”时出现 `[网络错误] This operation was aborted`。86毫秒确定性红测证明内部30秒模型时限主动Abort后被错误包装成网络故障，并泄漏英文DOMException；同时产品身份/能力问题被错误交给0.5B模型，真实第二轮曾生成成段重复垃圾。修复后能力问题由本地产品能力合同零模型调用返回，覆盖播放/下载/字幕/拉片/文档/剪辑/模型与审批边界；内置模型普通生成时限调整为120秒、其他loopback为90秒，真正超时统一显示中文`模型响应超时`，用户主动停止仍为`已取消`。原始安装态复现脚本同时走直接IPC和真实聊天框各连续两轮，修复前精确红出原英文错误，修复后四次回复均为完整中文功能清单，第一/第二直接调用11ms/234ms，`exactAbort=false`、`timedOut=false`。正式`pnpm check`为797 tests、797 pass、0 fail、0 skip，安全扫描current434/history1701无发现；修复候选EXE/ASAR为`D0C36BC4321C3AAE3922BE1D7E5AA65FC1026156BAF150E7496E843332462E57`/`053E0254A338364EA96BAE3163FE1DAC7AEBE8CA4BB70FC3E7D09F023E200BE6`，仍为`NotSigned`，已覆盖桌面安装位并启动。

2026-08-23 第二个纵向切片（仍不足以勾选整项）：当媒体旁已有带时间轴的SRT字幕时，指令“删掉口头禅和重复”进入主进程`subtitle-cue-cleanup-v1`，字幕时间轴在规划阶段一次读取并冻结为删除证据，不依赖模型临场猜测。安全边界仅删除时长不超过2.5秒、整条内容完全由“嗯/呃/额/啊/那个/这个/就是/然后/怎么说/你知道吧/对吧/是吧”组成的独立字幕，以及3秒内紧邻出现、规范化文本长度至少4字的完全重复句；“然后我们继续”等嵌在有效语句中的词、非紧邻重复和语义近似句均不自动删除。没有现成时间轴字幕时明确要求先生成字幕，不以无证据转写冒充完成。冻结方案继续交给`EditDecisionList v1`和`media.edit-concat`持久任务，视频与字幕原件均不覆盖。安装态同一会话分别执行长停顿清理和字幕清理：字幕切片删除2处，10.04秒原片生成8.222秒成片，质量95通过；“撤销刚才的剪辑”恢复10.04秒，原视频哈希不变。正式`pnpm check`为799 tests、796 pass、0 fail、3项外部网络条件跳过，安全扫描current434/history1706无发现；候选EXE/ASAR SHA-256为`CB410011CF461D7611C438B3AA233C61578C6A55260F3E70FEC290EC185CC9AE`/`598139E0E0CC46DBE5C30A78B2FC766B7F75A44668416D8647D826EDB9D9E65D`，仍为`NotSigned`。句中口头禅、非紧邻或语义重复、跑题识别和五类素材人工标定尚未完成，因此A1和0.9.1总完成率继续保持0%。

2026-08-23 第三个纵向切片（仍不足以勾选整项）：`subtitle-cue-cleanup-v1`继续扩大到“句中疑似口头禅定位”和“非紧邻完全重复确认”两条不同置信度路径。句中“就是，……”等内容只有字幕条级时间轴、没有逐词时间戳时，只显示字幕序号、起止时间、原文和命中词，不允许为了删几个字而删除整句；非紧邻但规范化文本完全相同的整条字幕可形成冻结删除候选，但必须先显示方案并等待用户明确回复“确认执行”，确认前不创建持久任务，取消或切换视频即失效。确认后仍执行同一份带哈希EDL，不重新分析。隔离安装态在10.04秒音画夹具中发现3个可删除候选（独立口头禅、相邻完全重复、非紧邻完全重复）和1个仅标记的句中口头禅；确认前`media.edit-concat`任务数为0，确认后生成7.222秒成片、质量95通过，回执明确说明1条未删除项，撤销恢复10.04秒且源哈希不变。正式`pnpm check`为801 tests、801 pass、0 fail、0 skip，安全扫描current434/history1715无发现；候选EXE/ASAR/安装包 SHA-256为`35765A82675F966E6CB81E98FA6AA0E6E981BC19AF04FD4D1DCF56EE4EF39BAF`/`5112AA9B2EA9C6AA3C07664349E4E09C4E9F4D055E5886DD04E06DBB594774C4`/`65DCE01247CC9BF31914121E59D7E8D77598C8F66C5B6EB610BEAE48FBB30515`，仍为`NotSigned`。逐词时间戳生成、语义近似重复、跑题证据与五类素材人工标定仍未完成，所以A1和0.9.1总完成率保持0%。

2026-08-23 第四个纵向切片（仍不足以勾选整项）：复用已经安装的whisper.cpp tiny模型生成真实词/token时间，不增加第二套模型包。逐词模式固定使用完整JSON、`DTW tiny`和关闭flash-attention；本机审计证明不关闭flash-attention时`t_dtw=-1`，关闭后“然后”获得真实11.74–12.58秒边界，不能拿整句时长按字符比例均分冒充精准。规划器只对SRT已标记的句中候选抽取局部音频，每条保留1秒前文避免Whisper吞掉解码首词、0.5秒后文形成下一词边界；单次最多4条，优先tiny保证速度，只有识别词与SRT命中词完全一致、落在原字幕时间范围附近、置信度不低于0.15且词长0.08–1.5秒才升级为可删除候选，其余继续只标记。逐词候选仍必须先确认，确认前任务数为0；词边界、置信度、模型和方法同时进入`semanticCut`与EDL质量合同，篡改证据会被EDL一致性校验拒绝。安装态用Windows中文SAPI现场生成新视频，独立DTW证据为“就是”2.72–3.50秒，候选应用重算为2.62–3.48秒；确认后10.643秒原片生成9.869秒成片、质量95通过，撤销回10.643秒且源哈希不变。正式`pnpm check`为805 tests、805 pass、0 fail、0 skip，安全扫描current437/history1724无发现；候选EXE/ASAR/安装包 SHA-256为`99F3A57255359355092E9826B4DF8AAFD8EA5D7E33C39D67BAEDA80B7DE903AD`/`5E5DA89F708C8870481C423688037EDC029781E19BFD27919B182CCECF8F7334`/`001D75AE03B28C4E4EA7389CF3CFB751FA506A9B9F04510F7963EB5A8AABB28F`，仍为`NotSigned`。语义近似重复、跑题证据与五类素材人工标定尚未完成，因此A1和0.9.1总完成率继续保持0%。

2026-08-23 第五个纵向切片（仍不足以勾选整项）：语义近似重复和跑题不再用关键词规则判断，而是交给已配置的本机8B级、订阅或云端工作模型做结构化审阅；内置0.5B轻量模型明确禁止作为删片依据。云端/订阅模型调用前用原生确认框说明只发送字幕序号、时间和文字，不上传视频或音频；本机loopback模型无需上云审批。工作模型只能围绕最多80条编号字幕返回主旨、候选类型、相关字幕序号、建议删除序号、置信度、理由和原文连续引句；主进程要求每条引句真实存在、近似重复至少有两条证据且保留一条、置信度不低于0.85，单轮最多8个候选，建议删除总数不得超过字幕条数35%。模型输出不直接执行，只有通过合同的中间字幕才冻结为`model-semantic-review-v1` EDL并展示给用户，确认前任务数为0；模型身份、主旨、引句、置信度和删除范围全部进入`semanticCut`与EDL质量合同。隔离安装态以本机OpenAI兼容模型真实走一次请求，模型提出1条价格语义重复和1条火锅跑题候选；确认后10秒原片生成7.782秒成片、质量95通过，撤销回10秒，源哈希不变，模型调用次数严格为1。正式`pnpm check`为810 tests、809 pass、0 fail、1项外部网络条件跳过，安全扫描current440/history1736无发现；候选EXE/ASAR/安装包 SHA-256为`2A6607B2F8A578846FD3667C33AF5234A601FBA0F4240061DD0A1F1E4EE02352`/`F2FA36314F0B534778C3BF9B79E5C420EBC0F10CE61A5EE64B8BA99D9B74CB08`/`27A21392C1E1BED62A1BEDE019A65B9002A950A993BDF0F5E795B297AA4B4BC9`，仍为`NotSigned`。镜头证据交叉验证与访谈、课程、剧情、产品介绍、竖屏口播五类人工标定尚未完成，因此A1和0.9.1总完成率继续保持0%。

2026-08-23 第六个纵向切片（仍不足以勾选整项）：字幕模型提出并通过引用硬门的候选还不能直接进入EDL，必须再取得候选删除区间前0.25秒、区间中点和后0.25秒三张JPEG镜头证据；最多8个候选、24张图。视觉模型只判断删除是否会丢失独有产品演示、操作动作、图表证据、关键手势、人物反应或场景转折，以及前后能否连续衔接，不允许重新判断字幕语义。每个候选只能返回`safe/unsafe/uncertain`、置信度、观察理由和帧标签；只有`safe`、置信度不低于0.85且同时引用对应before/middle/after三帧的候选才保留，其余全部挡下。没有视觉模型、缺任一帧、输出缺标签或视觉不同意时都只显示候选而不创建任务；远端视觉模型调用前另行说明只发送候选关键帧，不发送整段视频。字幕模型和视觉模型身份、全部候选、三帧时刻、视觉结论和被挡候选同时冻结进`semanticCut`与EDL质量合同。隔离安装态通过本机Agnes兼容接口真实完成1次字幕请求和1次六帧视觉请求，两个候选均引用完整三帧并通过；确认前任务数为0，确认后10秒原片生成7.782秒成片、质量95通过，撤销回10秒且源哈希不变。正式`pnpm check`为814 tests、814 pass、0 fail、0 skip，安全扫描current442/history1747无发现；候选EXE/ASAR/安装包 SHA-256为`BEF44C11B006B8B722BC20F1A992B6A453967E5427DD6633EACC9DD739E553A3`/`C751AEC71724B408A54D454E8DE2D042BC36AB634D5A3961ACD1A4EF05815701`/`A786FAA53346B919FFF6241F64F4ED62BB6CD9C6E39056E4D25A580899400B3B`，仍为`NotSigned`。访谈、课程、剧情、产品介绍、竖屏口播五类真实素材人工标定尚未完成，因此A1和0.9.1总完成率继续保持0%。

2026-08-24 第七个纵向切片（A1闭环并正式勾选）：五类私有标定语料全部落盘且不进入Git，包括Internet Archive的CC BY-NC-SA访谈、公版有声课程，以及用户自有剧情、产品介绍和竖屏口播；每类包含原片负样本和复制真实片段形成的已知重复正样本，共10案例、20个视频/字幕哈希全部匹配。真实语料先发现并修复两项转写底层缺陷：Whisper整片SRT默认30秒一条导致访谈44秒仅2条、产品66秒仅3条，现固定`-sow -ml 36`形成有界短句；Whisper填充时间把44秒写到60秒、66秒写到90秒，现按实际解码WAV时长裁切越界cue并重新编号。云端标定经用户明确授权，文本和视觉统一使用真实`agnes-2.5-flash`；2026-08-24真实JPEG调用HTTP 200并返回文本与`reasoning_content`，推翻08-04“2.5不收图”的旧能力结论，2.0只保留明确拒图时的兼容回退。标定过程逐案例原子保存检查点并保持人工真值不变：首轮precision/recall为0.4/0.4且unsafe3；修复类型错标、相邻续句、装饰引号JSON和空响应后达到1.0/0.4；增加保留段参考帧后达到0.8333/1.0；最终为非完全相同的近似重复增加0.35字符锚点硬门，10案例得到TP5、FP0、FN0、unsafe0、processingFailure0，precision=1、recall=1。近似重复视觉证据现为before/middle/after加reference四帧，视觉单次最多6候选/24图；JSON合同只允许一次修复，空响应最多三次，且所有处理失败都计入标定失败。私有结果只留在`C:\Users\Administrator\Videos\AgentPlay 标定\0.9.1-A1`，公开仓库仅包含通用清单校验、评分器和显式云端开关。安装态2.5文本/视觉各调用1次、质量95、可撤销且源哈希不变；正式`pnpm check`为824 tests、824 pass、0 fail、0 skip，安全扫描current446/history1759无发现；候选EXE/ASAR/安装包 SHA-256为`EFD3F91FC19E9FD4993A7327555777759373EF664FE9132C281021891404F536`/`9ECCE8042297C1FD4519A242C19A169BC57BF4FC79B86662B1A9C74C728383C7`/`05FB00854106207994C6168BF740A2BBD5C20E0EB42BA5C064AECDC312AAC4D2`，仍为`NotSigned`。A1已达到“转写/音轨/逐词/模型引句/镜头证据、确认、持久执行、质量门和撤销”的闭环，因此0.9.1更新为1/25=4%；A5的全组五类标定仍需等A2–A4完成后再勾选。

2026-08-24 第八个纵向切片（A2首段，仍不足以勾选整项）：明确指令“从他说到‘某句原话’开始”优先进入主进程`subtitle-exact-quote-v1`，只使用相邻现成字幕的真实时间轴定位，不调用模型猜秒数。规范化原话在全部字幕中只能出现一次，且必须位于该字幕条起点，才冻结为`media.trim`决策；原话、字幕路径、字幕序号、整条文字、起止时间和剪辑范围一并进入EDL质量合同，任一字段被改写都会在执行前拒绝。出现多处时列出每处字幕序号与秒数并要求补充上下文；只命中字幕条中间时明确说明没有逐词时间戳，不把整条起点冒充原话起点；咨询式“能不能……”不会误执行。执行复用主进程持久`media.edit-trim`、原子另存、源指纹复核、帧边界证明、质量门、自动预览和对话撤销。正式`pnpm check`为826 tests、826 pass、0 fail、0 skip，安全扫描current446/history1777无发现。隔离候选从第6条字幕原话定位生成4.382秒成片，质量95，自动预览后撤销恢复10.04秒且源哈希不变；随后标准安装包成功覆盖`C:\Program Files\ai-player\AgentPlay`并运行，安装态EXE/ASAR/安装包 SHA-256为`BE096D18A2CED1909DB9D83D8382A9C5488F5F5EDFFB87D4A852577631E8F075`/`B5FB556BB76FA6FECEDF7E3FEA461161611BC644C9D1B93EAB46C786C99D6886`/`C59F01F80640904EAF1585BBEA9D45E7DCECDE3671D017E0FA6006AC82A8D5C9`，仍为`NotSigned`。A2还缺“保留讲产品价格的部分”等主题级语义定位，以及句中原话的逐词时间定位，因此A组仍为1/5、0.9.1仍为1/25=4%。

2026-08-24 第九个纵向切片（A2第二段，仍不足以勾选整项）：明确指令“保留讲产品价格的部分”进入主进程`model-topic-selection-v1`。工作模型只能从最多80条带序号和时间的字幕中选择直接回答用户原主题的条目，返回原样主题、置信度、字幕序号、理由和每条连续原文引句；主题被改写、引句不在原字幕、置信度低于0.85、选择整段、超过12条或形成超过6个零碎组时全部失败关闭。云端模型调用前说明只发送字幕序号、时间和文字，不上传音视频；内置0.5B轻量模型不得作为主题剪辑依据。相邻证据编成一次`media.trim`，分散证据按原顺序编成`media.concat-segments`；每段首尾保留0.08秒边界，模型身份、主题、证据、选择范围和确认要求同时进入EDL质量合同。模型结果不直接执行，界面先列出字幕引句、范围、模型和置信度，用户确认前任务数为0；确认后复用持久剪辑、原子另存、质量门、自动预览和撤销。隔离安装态以本机OpenAI兼容端点完成2次文本请求（原语义审阅与新主题定位）和1次视觉请求；主题定位选择第2、3条价格字幕，10秒原片生成2.782秒成片、质量95，撤销回10秒且源哈希不变。正式`pnpm check`为831 tests、831 pass、0 fail、0 skip，安全扫描current447/history1786无发现；桌面标准版已覆盖并启动，安装态EXE/ASAR/安装包 SHA-256为`2165E3074421240B2DB8DA3D9E37D0038D90F977B4944C013AFAE1E644A46EF1`/`3079DB9D537C5014FEC94A83AC257C10EBFD1A2DE2B3E8E06A6813A4350FA1E1`/`435FFD077150BA3F26DB576B78197D99C9D9B7EAA9FECE7E9070B26C7E9FA88F`，仍为`NotSigned`。A2现在只剩“原话位于一条字幕中间”时复用真实逐词时间定位起点，完成前A组仍为1/5、0.9.1仍为1/25=4%。

2026-08-24 第十个纵向切片（A2闭环并正式勾选）：当唯一原话只位于一条字幕中间时，规划器不再停留在整条SRT时间，而是把该字幕前1秒至后0.5秒的局部音频交给已安装的Whisper tiny，使用`DTW tiny + -nfa`取得真实词边界。`whisper-dtw-phrase-start-v1`要求完整短语从真实DTW token边界开始、跨越连续词、在局部音频中只出现一次，最低词置信度0.15；起点落在聚合token内部、短语不完整、出现多次、时间越界或转写组件不可用均停止，不按字符比例猜秒数。短语起止、最低置信度、词数、模型和方法全部进入`semanticLocate`与EDL质量合同，篡改任一字段都会在执行前拒绝。真实验收同时发现whisper.cpp Windows JSON会把部分中文byte-token写成`�`，但同一segment完整文本仍正确；现仅当连续损坏DTW组数量与两侧真实锚点间缺失Unicode字符数严格一一对应时恢复词面，否则保持损坏并拒绝定位，禁止自由补字。Windows SAPI现场生成“欢迎大家。就是，今天我们介绍产品……”音画夹具，独立DTW证据得到目标短语3.50–6.32秒，安装态规划得到3.50–6.34秒，起点完全一致、终点差0.02秒；生成7.206秒成片、质量95，撤销恢复10.643秒且源哈希不变。全量首轮还暴露测试运行器默认同时启动过多真实ffmpeg文件，导致本应31秒通过的跨素材拼接两次耗尽180秒；现默认文件并发固定为2、环境覆盖也限制在1–4，隔离复跑31秒通过，最终全量稳定通过。正式`pnpm check`为837 tests、836 pass、0 fail、1项外部网络条件skip，安全扫描current447/history1798无发现；标准桌面版已覆盖并启动，EXE/ASAR/安装包 SHA-256为`AE587493301110F587ACE9A56B97A90B4EE50B33B9A3FDE71DF5AF7890C04F78`/`A3A5E9090FCD1AE034DAA657A57171F3B698213BA811F9DADA3F5E6221A623B0`/`E74C6538F6639D798738F60680F6281C5903A3BB1AF747E402D6BA4848AE5B1C`，仍为`NotSigned`。至此A2的字幕条起点原话、句中原话和模型引用主题保留均完成持久执行、质量门、预览与撤销闭环，A组更新为2/5，0.9.1更新为2/25=8%。

2026-08-24 第十一个纵向切片（A3闭环并正式勾选）：新增统一`media-auto-inspection-v1`，明确指令“自动检查这个视频并给我剪辑方案”一次汇总五类证据。音轨复用`silencedetect`识别片中超过0.9秒停顿并保留0.12秒呼吸边界；字幕只把不超过2.5秒的独立口头禅和紧邻完全重复列为安全删除，句中词与非紧邻重复继续只标记。画面用`blackdetect`识别至少0.4秒、占比98%的内部黑帧；`blurdetect`按最高每秒2帧取得逐帧模糊分数，只把同时超过绝对8和全片中位基线1.8倍、连续至少0.5秒的区间标为明显失焦候选。重复镜头使用最高240张32×18灰度帧，只接受相隔至少2秒、连续至少1.5秒、逐帧平均差不超过3且原序列内部确有画面变化的后段复制，静态口播不会因单帧相同被误报。安全批处理仅合并内部长停顿、独立口头禅/紧邻重复和明显黑帧；失焦、重复镜头、句中口头禅与非紧邻重复全部`reviewOnly`，原因是相同画面可能承载不同对白、失焦也可能是创作意图。方案先列出每个安全区间、仅审阅区间、预计压缩时长，确认前任务数为0；确认后冻结同一EDL交给持久`media.edit-concat`，另存、质量门、自动预览和撤销保持不变。真实9秒音画夹具含1秒黑帧/静音、独立“嗯”、2秒boxblur和后段动态镜头复制；安装态检测1段黑帧、2段失焦、3段重复候选，合并为2个安全删除区间、5个仅审阅项，确认后生成7.624秒成片、质量100，撤销恢复9秒且源哈希不变。正式`pnpm check`为843 tests、843 pass、0 fail、0 skip，安全扫描current451/history1811无发现；标准桌面版已覆盖并启动，EXE/ASAR/安装包 SHA-256为`C937866EEB7396E27F76B7312DB0BA2BDCE2908B46F45BC76B673DEB11302868`/`9A50A018B5AFC5C741D7F866EBDCE368F7E9DB8793AB690F6DD34C808D443B92`/`DED48B144FB8CE3575A1219525BD09687805EB544A0DBBD7FE45EE4B1F6EF583`，仍为`NotSigned`。至此A3完成，A组更新为3/5，0.9.1更新为3/25=12%。

2026-08-24 第十二个纵向切片（A4闭环并正式勾选）：新增`shared-evidence-long-video-versions-v1`。明确指令要求长视频短版/精华版/章节版/平台时长版时，只把最多240条字幕序号、时间和文字交给一个工作模型一次；模型只能返回2–12个按序、无重叠且连续覆盖全片的章节，以及2–24个高光范围。每个章节与高光都必须引用范围首尾字幕的连续原文，重要度0.5–1；缺章、重叠、伪造引句或范围越界时只允许一次合同修复，仍失败则不创建任务。主进程从同一共享证据确定性编译短版30秒、精华版90秒、15/30/60秒平台版，以及逐章独立片段；高光按重要度选入预算，再恢复源片顺序，最后一段只按剩余预算截断，任何版本不得超过目标时长。方案与SHA-256`planHash`冻结后先显示内容摘要、每个版本计划时长、章节范围和唯一模型身份，确认前任务数为0。确认后进入`media.version-bundle`持久任务，源视频与相邻字幕均做身份/大小/mtime/首尾内容指纹快照；每完成一个版本即保存检查点，崩溃后只从未完成成果继续。每个输出继续复用现有trim/concat、EDL、原子另存、真实时长与帧边界证明；多个成果不覆盖原片，任一成果可作为下一轮编辑来源。真实72秒音画夹具含12条字幕，安装态模型严格只调用1次，生成5个时长版和3个章节片段共8个MP4；实际时长为30.024、36.024、15.024、30.024、36.024秒及三个24.024秒章节，全部可打开、质量100，自动预览首个短版，原片哈希不变。正式`pnpm check`为850 tests、850 pass、0 fail、0 skip，安全扫描current455/history1824无发现；标准桌面版已覆盖并启动，EXE/ASAR/安装包 SHA-256为`D28EABED7BEFD0D684A5A09669FC833146E75CDDB9B4D4546FA948EF6E154CBF`/`7607DF33DF0FA94B654D948E9494831375CACEF001A8099E13EE815AE56B8C07`/`D2205D86F70088B9AB1C8800F04EDBFA41C53B77F3B314FBC6B0E5E89A25AEF1`，仍为`NotSigned`。至此A4完成，A组更新为4/5，0.9.1更新为4/25=16%。

2026-08-24 第十三个纵向切片（A5闭环并正式勾选）：复用A1不进Git的五类私有真实语料，在访谈、课程、剧情、产品介绍和竖屏口播上为A2–A4分别冻结人工真值：唯一原话及字幕序号、主题直接相关字幕序号、派生复制片段，以及多版本高价值锚点。每类同时跑原话定位、主题选择、自动体检和共享证据多版本四条线，共20条能力线；主题按人工真值计算precision/recall，体检要求派生复制由连续动态镜头或字幕时间轴任一真实证据命中且失焦/视觉重复绝不进入安全删除，多版本要求章节连续覆盖、至少半数人工锚点进入高光，并真实导出短版与一个章节片段。五轮标定先后暴露并修复：云端空响应/网络瞬断需有界重试；模型返回装饰引号会破坏JSON，章节/高光首尾证据改由主进程按合法序号重建完整源字幕；固定私有输出名会拿旧成片验证新计划，改为按冻结决策哈希命名；可变帧率真实视频的成片/源片PTS网格不同，帧证明改在严格相邻帧候选中找同内容，0.5秒错切红测继续被拦；字幕末尾最多超真实媒体0.232秒时按媒体时长裁住；主题原文精确出现时走确定性证据，不消耗模型调用。最终五类私有验收为quote5/5、derivedDuplicate5/5、versions5/5，topic TP21/FP0/FN1、precision=1、recall=0.9545，unsafeVisualDeletion0、processingFailure0、minimumExportQuality100，私有源视频/字幕20份哈希全部不变。最终结果与SHA-256回执只留私有目录，公开仓库仅包含通用schema、评分器、显式云端开关和无私有路径/Key测试。最新版安装态再次通过句中原话质量95及一次模型调用生成8个多版本成果质量100；正式`pnpm check`为855 tests、854 pass、0 fail、1项外部Archive网络条件skip，安全扫描current459/history1839无发现。标准桌面版已覆盖并启动，EXE/ASAR/安装包 SHA-256为`95D92ACFBB28E36703228C3007C1E32BC52A9A58E5EEC9A640CB8A9CA7C5F9B8`/`C47F12DA699D3537DD2A11FAED7627A08B720FBBBA8B2CC1230085AB766CA207`/`DD99D79DD3127C04ECCA0E6C242085570F9637A6612F088D56BB007ABAB9E1F9`，仍为`NotSigned`。至此A组5/5闭环，0.9.1更新为5/25=20%。

#### B. 专业画面编辑（5/5）

- [x] 支持裁切、缩放、画中画、关键帧运动、转场、遮罩、模糊和基础调色的自然语言控制。
- [x] 自动生成 16:9、9:16、1:1 版本并跟踪主要人物/主体，允许用户一句话修正跟踪对象。
- [x] 支持防抖、旋转、曝光/色彩校正和低质量片段提示，处理前保留可比较原版。
- [x] 可按拉片报告复用镜头节奏、景别结构和视觉风格，但不逐帧复制受版权保护作品。
- [x] 所有效果在真实导出后检查分辨率、比例、黑边、黑帧、编码和画面完整性。

2026-08-24 第十四个纵向切片（B1闭环并正式勾选）：新增`media.visual-effects`冻结决策与`media.edit-visual-effects`主进程持久任务，一句中文可组合控制16:9/9:16/1:1居中裁切、0.5–3倍缩放、带本地素材与位置/大小/时段的画中画、推近/拉远/左右平移关键帧、指定秒点和时长的叠化转场、位置与时段遮罩、分时模糊，以及亮度/对比度/饱和度/冷暖调色。多个时段按各自分句绑定，画中画1–5秒不会再误套到遮罩1–4秒；缺画中画素材或转场时间只追问唯一缺项。所有效果进入统一EDL、素材指纹快照、非覆盖输出、项目版本、质量门和崩溃恢复；恢复时从源片与冻结裁切比例独立推算目标尺寸，不信任现成成果自报尺寸。真实ffmpeg测试覆盖六效果组合、画中画+同步音频转场以及伪造错误尺寸成果拒绝。安装态一句话同时执行8类效果，6秒320×180源片生成100×180、5.514秒新片，质量100，自动预览后撤销回6秒；源片与画中画哈希均不变。验收同时发现`build:electron:lean`会在前序测试失败时把旧`dist`与新主进程混装，表现为主进程规划成功但旧界面把效果请求错误送往云端；标准版和完整本地AI版现都强制先执行`build:web`，回归测试禁止复发。正式`pnpm check`为865 tests、865 pass、0 fail、0 skip，安全扫描current464/history1851无发现；标准桌面版已覆盖并完成同一安装态闭环，EXE/ASAR/安装包SHA-256为`9F0CCFF91E835505C140D42397E0C5289B8CCA1C71E5D8FA7686D5281F684232`/`6CFAA4DDD955D7112F0B3D9C4A7438DF70B2BDC4257996BA2517251236A47C44`/`604FB0AB694AC0B80FD85F310CAE679414BE52BC045DF5AAC39EE3A831A17190`，仍为`NotSigned`。至此B组更新为1/5，0.9.1更新为6/25=24%。

2026-08-24 第十五个纵向切片（B2闭环并正式勾选）：新增`media.smart-reframe`冻结决策与同名主进程持久任务。明确要求三比例与主体跟踪时，只抽取全片均匀5张带标签JPEG关键帧交给当前视觉工作模型；远端模型调用前说明只发送5帧、不上传整段视频或音频。模型必须对用户指定的同一对象逐帧返回0–1归一化最小外接框与置信度，五个标签缺一、重复、越界或任一置信度低于0.75均停止，不用画面中心冒充主体。主进程冻结请求对象、实际观察、模型身份、五框和三种目标尺寸，再在关键帧之间线性插值裁切窗口；640×360原片确定性导出640×360横屏、202×360竖屏和360×360方形，不放大原像素。任务支持逐成果恢复、三成果格式/时长/尺寸/主体覆盖质量门、原件指纹复核与可撤销项目；三个关联成果中的任意一个都能用“改为跟踪某对象”回到原片重新分析，不在已裁成片上二次裁损，旧对象和模型写入`correctionOf`。真实双移动对象安装态先跟踪红色对象生成三版，再从竖屏成果一句修正为蓝色对象重新生成三版；两轮质量均100、视觉模型严格2次、源片哈希不变。独立像素门不信任模型与计划回执：两轮竖屏成片中红/蓝目标像素质心分别为x=50/101与x=50/101，证明最终编码确实跟随指定对象；初版只验计划几何时曾让背景居中，该反例已被像素红测挡下。正式`pnpm check`为873 tests、871 pass、0 fail、2项Archive外部网络条件skip，安全扫描current469/history1870无发现；候选EXE/ASAR/安装包SHA-256为`6F5812911DE6FC97D58C9FDB70D3047B564BC3BC4A62B46964C4ECEFB8317BB7`/`2383C59ED6077C856BF1DB4570262124059FED4AF8378B0AEC49D8E4FB8DE7FB`/`4577306C1BBF6880B348CF136FB3BB72D77114C4CA2A85E015B520BADCE86405`，仍为`NotSigned`。至此B组更新为2/5，0.9.1更新为7/25=28%。

2026-08-25 第十六个纵向切片（B3闭环并正式勾选）：新增`media.visual-repair`冻结决策与主进程持久任务。自然语言可组合防抖、顺/逆时针90度或180度旋转、自动曝光/饱和度/白平衡校正和低质量片段提示。防抖使用ffmpeg vidstab两遍处理，第一遍ASCII变换文件按每帧局部运动向量中位数计算真实运动幅度，修复版再检测一次；明显抖动画面未降低至少10%即拒绝交付，原片运动很低时诚实标记`not-needed`。自动色彩只使用signalstats均匀采样的YAVG/UAVG/VAVG/SATAVG，亮度、对比度、饱和度和红蓝平衡均有界；修复后到中性目标的统计距离未降低至少5%即拒绝。黑帧、失焦和疑似重复镜头沿用A3证据，只作为`review-only`提示，绝不随修复任务自动删除。方案先显示修复动作、仅提示区间和“原片不覆盖”，用户确认前任务数为0；确认后另存画面修复版，并把原版和修复版等比放进同一画布生成并排对比版，两成果进入同一可撤销项目。真实5秒抖动、欠曝、蓝偏且含黑帧夹具安装态质量100：运动幅度11.561→6.466（改善44.1%），曝光偏色中性距离57.304→22.690，顺时针90度成片240×360，对比版720×360，2个低质量区间仅提示，撤销回360×240原片且哈希不变。测试同时证明vidstab需显式`fileformat=ascii`才能得到可审计变换文本；Windows滤镜路径须正斜杠并转义盘符冒号。正式`pnpm check`为881 tests、878 pass、0 fail、3项外部网络条件skip，安全扫描current474/history1889无发现；候选EXE/ASAR/安装包SHA-256为`79DDC8A4BBC1359A96ABC9ED7BF2B8EA0DF54ECE637AC56DCA296AB37980917A`/`1D60E757228B84A336053B174A4E29C303FB6A0FF40D8FBCC9C0B40075CA2E71`/`BFB59E47FE10A586EB301424DEBC9219B6389C23D2CB4D4A88FD35FC6D20D665`，仍为`NotSigned`。至此B组更新为3/5，0.9.1更新为8/25=32%。

2026-08-25 第十七个纵向切片（B4闭环并正式勾选）：重构短片不再把整份拉片报告直接交给模型自由模仿。本机`abstract-style-blueprint-v1`只从报告提取1–8秒镜头时长序列、景别、运镜，以及白名单色彩/光线/构图类别，并只在蓝图中保留报告SHA-256；人物名、品牌、Logo、引号原句、片名和独特构图坐标进入独立保护清单，不写入蓝图，也不发送给镜头模型。镜头模型只接收抽象蓝图和新的原创目标，严格返回与蓝图同序的duration/shotSize/movement及原创声明；数量、时长、景别或运镜错位，提示词包含逐帧/照搬/原片人物/Logo/画面文字，或命中任一保护片段时整单失败，不能靠模型自称原创过关。既有`creative.recut-short`持久任务、逐镜头Agnes生成、远端video_id恢复和ffmpeg拼接继续复用；检查点新增`styleShots`与`styleReuseReceipt`，成片质量门要求蓝图、结构匹配、提示词哈希、`rawReportSentToShotModel=false`和`referenceImagesSent=0`同时成立。界面在付费生成前先显示本机蓝图边界；付费审批文案明确只发送抽象蓝图与新主题，不发送报告正文或参考帧。安装态受控报告保留节奏2/3/4秒、景别中/近/特、运镜固定/推/跟，同时确认Lindy、AcmeAI、英文原句和Logo不在蓝图；三个社区图书交换原创镜头通过，含“逐帧复刻 AcmeAI Logo 和 Lindy 原片构图”的镜头被拒，蓝图哈希篡改也在创建付费任务前失败且任务数不变。完整云端成片仍沿用已验证的重构生成链，本切片没有为合成验收额外消耗付费视频额度；新增合同证明生成阶段只消费已验证安全镜头。正式`pnpm check`为887 tests、886 pass、0 fail、1项外部网络条件skip，安全扫描current478/history1907无发现；候选EXE/ASAR/安装包SHA-256为`E1BB2A1DEF625CA190BDFB5004F8D839E212362C44714645F11A373B42CD8A03`/`F7F0C3149B2ED07740ADED751C2B0E85219808BD6C64EDF11026E66B531ECAA7`/`4F96FB4D9DB6B886EE718897BDB8E2A0D6BB0F9F974113E67AD9589406087486`，仍为`NotSigned`。至此B组更新为4/5，0.9.1更新为9/25=36%。

2026-08-25 第十八个纵向切片（B5闭环并正式勾选）：新增`unified-visual-export-qc-v1`作为B1–B4全部视觉导出的共同交付闸门。每个成果用ffprobe核对尺寸、显示比例、H.264、yuv420p、SAR 1:1和帧率，再完整解码视频流；最多以2fps/60帧的160像素宽灰度样本计算全黑帧、上下/左右黑边和连续完全冻结。源片存在时先冻结同法基线，只允许保留源片已有问题，不允许新增超过1个黑帧采样、超过20个百分点且覆盖至少70%采样帧的黑边，或比源片新增0.75秒以上且总长超过2秒的长冻结。B1显式缩小形成的内嵌黑边和B3前后并排对比的等比留黑必须在任务规格中显式`allowBlackBars=true`；普通视觉效果、B2三比例和B4原创重构不得借此放行。B1八效果、B2红/蓝两轮三比例、B3修复/对比和B4原创重构执行器均在写项目历史或交付前调用同一个服务，失败会移除任务自产物并拒绝交付；四类质量评分都新增15分统一QC项。真实缺陷红测确认：干净H.264/yuv420p放行，错误尺寸、MPEG-4编码、新增黑边和新增黑帧分别返回`DIMENSION_MISMATCH`、`UNSUPPORTED_VIDEO_CODEC`、`UNEXPECTED_BLACK_BARS`和`NEW_BLACK_FRAMES`，显式允许黑边时只豁免该项。候选安装态实际覆盖9个视觉成果：B1一个、B2红/蓝两轮六个、B3修复/对比两个，全部quality100且统一QC通过；B4成片执行器同样接入此门，受控安装态继续验证其蓝图和提示词边界，本轮未额外消费付费视频额度。正式`pnpm check`为890 tests、888 pass、0 fail、2项Archive外部网络条件skip，安全扫描current481/history1921无发现；候选EXE/ASAR/安装包SHA-256为`B90E23D411180F20065E39A844DC47EB06CEFA73FEE07DDE47CBDB5CB5921DDF`/`B56680E9A58940EB7C8B7169EC8E1F016A76E02AA8D653E5DA8A6C4BB25619F1`/`660AA8789D5C05ECFED4496321819E59B13F19C1469C329E363AFBE14C2443F2`，仍为`NotSigned`。至此B组5/5闭环，0.9.1更新为10/25=40%。

#### C. 专业音频与音乐（5/5）

- [x] 支持多轨对白、音乐、环境声和音效的增删、对齐、分段音量与自动闪避。
- [x] 支持降噪、去直流、响度匹配、静音修复和基础人声/伴奏分离，并如实提示模型伪影。
- [x] 支持按节拍切镜、音乐高潮对齐和片尾自然收束，用户可用语言调整“更快/更克制”。
- [x] 接入可核验许可证的公版/授权音乐库，记录曲目、表演/录音来源、许可证和使用范围。
- [x] 导出前检查削波、响度、声画同步、静音段和版权来源，失败时阻止伪装成功。

2026-08-25 第十九个纵向切片（C1闭环并正式勾选）：新增 `multitrack-audio-mix-v1` 与 `media.mix-audio`，一句话可把源片对白、本地音乐、环境声和音效冻结为统一 EDL；每条外部轨记录角色、目标起止、循环、基础音量、淡入淡出、对白闪避和分段音量，支持从最终总线移除原声。用户直接拖入多个音频时，界面要求按附件顺序说明用途，不按文件名猜；素材路径只在内部冻结，日常入口仍是一句话。主进程 `media.edit-audio-mix` 持久任务同时快照视频与最多8条音频，复杂循环输入先渲染为有限48k PCM总线，再做两遍 loudnorm 与AAC封装，避免第二遍编码等待无穷输入；输出另存并进入可撤销项目。`decoded-multitrack-pcm-v1` 从最终成片逐轨解码，在计划时间窗与轨道源PCM做有界延迟相关，轨道缺失/错位、静音、过载、自动化或闪避回执不完整、编码后响度不符均故障关闭。真实服务夹具用440Hz对白、220Hz音乐、330Hz环境声和880Hz音效验证3轨出现时间、3–4秒对白降音量、4–5秒音乐降音量、对白出现时音乐实际闪避；另一个夹具实测移除440Hz原声后只保留220Hz音乐，两轮所有源哈希不变。安装态从真实对话输入同一句话创建持久任务，3/3外部轨对齐、2段音量自动化、2轨对白闪避、编码后-15.9 LUFS/-12.9 dBTP、质量100、自动预览与可撤销项目全部通过。正式 `pnpm check` 为911 tests、910 pass、0 fail、1项Archive.org外部网络条件skip，安全扫描current492/history1948无发现。候选与桌面安装态 EXE/ASAR SHA-256 一致，分别为 `37BAEB24FA538BB3C35F7B254BA426F51D6ACBD774C37D1581BE4EF48DC11F47` / `4AC2A4DA21E1B2248739B228650A961BBB9E4B6F3AECC88D2C4F8AE24ED519E5`；安装器/便携包为 `66D22A917D716884FF55FFE1CA50CA7C224B6F868B3F4F83071288BFFED7DED5` / `080013D3BA988FC7B49810D6414D0DA1E71ECB77C67E922FC2D073EC270860FF`，仍为 `NotSigned`。至此C组更新为1/5，0.9.1更新为11/25=44%；C2降噪/修复/分离与C4授权曲库仍按后续独立项推进。

2026-08-25 第二十个纵向切片（C2闭环并正式勾选）：新增 `ffmpeg-audio-repair-v1` 与 `media.repair-audio`，一句话把自适应频谱降噪、20Hz高通去直流、目标响度、短数字静音修复和可选基础人声/伴奏分离冻结进统一 EDL，并由 `media.audio-repair` 主进程持久任务执行、恢复和质量修复。静音修复只在片中检测到的 0.05–0.30 秒数字断音区间加入低电平粉红房间底噪，回执固定 `restoresSpeech=false`，绝不宣称恢复已经丢失的对白；基础分离只对立体声使用中置/侧声道差异，单声道故障关闭，界面和成果回执明确提示混响、偏置人声和中置乐器可能串入、轨道可能变薄，且“不是AI专业分轨”。执行先写有限 48k PCM，分别测量修复前后噪声 RMS、直流绝对均值和断音填充，再做两遍 EBU R128 loudnorm；AAC 编码后独立复测，必要时只在预测峰值安全且不超过6dB的范围内做一次有界总线校正。真实6秒立体声夹具把噪声 RMS 从0.085679降至0.056920（改善33.57%）、直流绝对均值从0.056570降至0.000231，修复1处0.156秒断音并达到-16.0 LUFS/-10.3 dBTP；人声干线以440Hz为主、伴奏干线以220Hz为主，单声道分离红测拒绝。安装态对话执行生成修复成片与两条WAV干线，质量100、项目可撤销、源哈希不变。首轮CI同时暴露旧重复文件测试在Windows上于读取流`end`后、文件句柄`close`前删除目录的竞态；新增延迟`close`红测后，哈希完成改为等待句柄关闭，旧场景压力复跑100/100通过。最终完整 `pnpm check` 为925 tests、924 pass、0 fail、1项Archive.org外网条件skip，安全扫描current499/history2000/packaged5均无发现。最终候选与桌面安装态 EXE/ASAR SHA-256 一致，分别为 `CBB7EA79A6787E1EE1F9DB4CDA92F3D2C1C1C5A0DF71048B587382382499F202` / `30734596A95E84C24F978E36C2480469992418E70C041CF4C966455E23EC1A59`；安装器/便携包为 `D29FA51B0CAD5AA441CC4A73247D5F77590D85D3F920D13C40837203F6CBD9B9` / `3467794F610FB87266ABC5616206B6A9B1A8C8CD45747E4A4CD5D006BFDCD5C9`，仍为 `NotSigned`。至此C组更新为2/5，0.9.1更新为12/25=48%；C3节拍/高潮对齐、C4授权曲库与C5总线导出审计仍按独立质量门推进。

2026-08-26 第二十一个纵向切片（C3闭环并正式勾选）：新增 `decoded-pcm-onset-grid-v1`、`beat-synced-jump-cut-v1` 与 `media.rhythm-edit`。用户拖入或明确提供本地音乐后，主进程先解码最多10分钟的11.025kHz单声道PCM，以短窗RMS相对历史增量寻找起音，按60–200 BPM候选间隔投票并从真实起音选择相位；起音不足8个、网格支持率低于45%、音乐或视频短于6秒均停止，不用固定秒数冒充卡点。`更快/均衡/更克制`分别冻结普通段每2/4/8拍切镜，高潮区每1/2/4拍切镜；高潮由滑动窗的响度与起音强度共同选出，并强制从最接近高潮起点的真实拍重新取更密网格。每个镜头在源片顺序上加入0.14/0.09/0.04秒有界跳切，视频、原声和音乐一起进入统一EDL与主进程持久快照；执行前先展示BPM、支持率、镜头数、高潮区和片尾强拍，确认前任务数保持0。片尾结束点必须是真实拍，画面与最终声音总线同时淡出。质量硬门从成片逐切点读取灰度帧、从高潮读取音乐/成片PCM相关，并比较片尾前后音频RMS与画面亮度；任一节拍证据、可见切镜、高潮密度、音乐对齐或双淡出缺失都拒绝交付。真实16秒120 BPM夹具中网格支持率100%，生成17个镜头/16个切点，高潮5.341–8.545秒，高潮平均切镜0.500秒、普通段0.907秒（密度比0.551）；安装态最终轮16/16切点可见、音乐相关0.044，片尾12.974秒强拍处1.2秒淡出，音频RMS 0.050612→0.006255、画面亮度115.781→2.423，质量100、可撤销，视频与音乐SHA-256均不变。首轮GitHub Windows门同时暴露旧WiFi上传测试在服务关闭回调完成前清理临时目录的竞态；`WifiTransfer.stop()`现返回可等待Promise并主动关闭空闲连接，真实上传先消费响应体再等待关闭，Windows清理压力复跑100/100通过。最终完整 `pnpm check` 为940 tests、940 pass、0 fail、0 skip，安全扫描current508/history2032/packaged5均无发现。候选与桌面安装态EXE/ASAR SHA-256一致，分别为 `22DC1470D9A5D178B47910EB46E9BE905694D35F9DC2890E3F0E63FB6D8FD7DB` / `FFF4A94466E91B9FFCE5C939EF691ABC9D153A50CE8A4BA8E60D5158353165C8`；安装器/便携包为 `A8489D9E6183466E2FDC323D4F4EE16E6AF00FB98C49FA0426833AAD0CC319C3` / `0A773E9A84B4A6CB887A7BF8FCA219A45A895191F1FB7D014AD3EB2EAE980E77`，仍为`NotSigned`。至此C组更新为3/5，0.9.1更新为13/25=52%；C4授权曲库与C5总线导出审计继续保持未完成。

2026-08-26 第二十二个纵向切片（C4闭环并正式勾选）：在线媒体库新增 `internet-archive-license-filter-v1` 可商用配乐入口，服务端检索和主进程下载前二次核验都只放行 Public Domain Mark 1.0、CC0 1.0、CC BY 3.0/4.0；音乐与画面同步在 CC 规则中属于改编，因此 NC、ND、SA、未知或缺失许可证全部故障关闭，不以“古典曲谱公版”冒充“现代录音公版”。每条结果和文件均记录曲目、表演者、原始录音来源、Archive 来源页、规范许可证 URL、商业使用/改编/视频同步/署名范围；下载不信任渲染器传回的直链或许可，而是按条目与文件名重新拉取元数据，落盘后流式计算 SHA-256，并生成相邻 `agentplay.licensed-music-receipt` `.license.json`；可转发凭证只保留本地文件名、字节数和哈希，不泄露用户绝对目录。界面只显示人能理解的使用范围和来源，“用于剪辑”下载后回到唯一主对话，预填本地音乐路径、对白闪避和不覆盖原片，不新增第二套编辑器。首轮安装态展开条目暴露 Archive 瞬时 `fetch failed` 未被旧重试器识别；新增断连红测、一次有界重试和中文稳定错误后复测通过。最终安装态真实搜索 `calm piano` 得到24条公版/CC BY结果，下载 `Corporate Light 3` 3,782,661字节，凭证记录 Megis、Jamendo 原始来源、CC-BY-3.0、商业使用与视频同步允许，凭证哈希 `a51bf74f97e192c62d551d51d1f63f3eee1c4d2e089164ab54b43ced73850466` 与MP3实算一致，且文件字段仅有 `name/localName/bytes/sha256`，剪辑输入自动预填。完整 `pnpm check` 为948 tests、948 pass、0 fail、0 skip；候选与桌面安装态EXE/ASAR SHA-256一致，分别为 `0897F0D45DA4DCE58735A0D7038803D91DEA04AB217C08142C79E1D8E0F318D4` / `22637297AE0D923483E1A3C14180B1AA7AAE41687A0B27EB47AE607B4FC59F14`，安装包为 `7A322AC2A84ED913BD596B603F9B445AA71BA146904CB3C4E80F8C5B10A1209A`，仍为`NotSigned`，不冒充稳定公开版。至此C组更新为4/5，0.9.1更新为14/25=56%；C5总线导出审计继续保持未完成。

2026-08-26 第二十三个纵向切片（C5闭环并正式勾选）：新增 `unified-audio-export-qc-v1` 作为普通配乐、C1多轨、C2修复和C3节拍剪辑的共同原子交付闸门；四类执行器在临时成片改名之前、冷启动恢复已有成果时都必须重新取得同一回执。闸门完整解码首音轨，以 sample peak 和 EBU R128 true peak 双重拒绝削波；有冻结响度目标时按目标与容差验收，没有目标时按专业包络检查，用户明确“不归一”则诚实记录 `matched-user-policy` 而不误杀，但仍不能绕过其他四项。ffprobe读取首个音视频流的起止时间，起点差不超过0.1秒、终点差不超过0.25秒；`silencedetect -60dB/0.5s`记录全部静音段，新增最长超过2秒或占比超过35%的静音失败，源片已有静音或显式只播放一次的计划尾静音单独标记。版权来源对C4授权音乐重新计算SHA-256并校验许可侧车、商业/改编/视频同步范围和无绝对路径；普通本地音频只记 `user-supplied-unverified` 与用户责任，绝不冒充已验证授权。统一回执进入任务质量评分，缺任一项返回硬失败 `UNIFIED_AUDIO_QC_FAILED`。安装态首轮还暴露旧C2仅因“响度归一”误抢“添加背景音乐”任务，新增组合句红测后把明确配乐动作留给配乐执行器。最终安装态在真实对话中生成4.023秒配乐成片，质量100：-16.0 LUFS、-11.6 dBTP，声画起点差0.023秒/终点差0秒、无异常静音、版权来源明确为用户提供未独立核验、回执无绝对路径，视频和音乐哈希均不变。正式 `pnpm check` 为959 tests、959 pass、0 fail、0 skip，安全扫描current514/history2047/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为 `0B71C9296114DD0B3A9B7F74AFD47CA181D5659898F2DD5F005F4D4716FA892F` / `F2E33A6F531B9C3230468F52126D3ED42FFB06BE3A61192A5CF14A97E975C8E3`，安装包为 `0A69AE64298DEAFB44F4168D54DA90E615337C97E7895904DCD14C21692FDDCA`，仍为`NotSigned`，不冒充稳定公开版。至此C组5/5闭环，0.9.1更新为15/25=60%；下一组按总路线进入D1字幕说话人/逐词高亮/卡拉OK/关键词/安全区能力。

#### D. 专业字幕与包装（5/5）

- [x] 支持说话人识别、逐词高亮、卡拉 OK、关键词强调和安全区自动避让。
- [x] 支持按品牌模板生成标题、章节条、人物条、角标和片尾，不强迫用户进入复杂设计面板。
- [x] 字幕可通过自然语言批量改字、改时间、合并、拆分、换语言和换风格。
- [x] 横屏、竖屏和多分辨率导出分别验收字号、行数、遮挡、断句和移动位置。
- [x] 预览与最终烧录逐条对齐，不能出现预览正确而导出错位。

2026-08-26 第二十四个纵向切片（D1闭环并正式勾选）：新增 `acoustic-speaker-karaoke-v1`，明确“专业动态字幕/逐词高亮/卡拉OK/说话人识别”等执行指令进入既有 `media.edit-burn-subtitles` 主进程持久任务，咨询、否定和举例文本不执行；视频与SRT均冻结指纹，恢复时重新核验，原件不覆盖。逐词时间只接受 whisper.cpp tiny 的真实 DTW JSON，并要求每条字幕与所选词逐字一致；不一致立即停止，不用字符比例猜时间。说话人只输出匿名编号，不冒充身份识别；每条字幕从真实词区间读取最多3.5秒PCM，以多短窗自相关基频、过零率和RMS形成声学向量，最多4组，冻结距离阈值0.18。渲染生成ASS `\kf` 逐词卡拉OK、说话人配色和显式/自动关键词强调；最多抽12个字幕时刻的真实画面灰度帧，比较顶部/底部复杂度选择安全区，成片再以源帧/成果帧像素差证明字幕确实落在所选区域。五项证据分别进入质量硬门，任一缺失均拒绝交付。真实安装态使用中文男女两套系统声线交替四句，独立夹具与应用均得到说话人序列`0,1,0,1`、置信度0.8825；29个DTW词对应29个卡拉OK标签，关键词强调4次，底部安全区像素差0.0319、对侧0.0005，质量100，视频与字幕哈希均未变化。最终完整 `pnpm check` 为967 tests、967 pass、0 fail、0 skip，安全扫描current517/history2064/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为 `34CBBCF1408B6BA2420B082E514ADBAC2D944A618A5934B85736DF388C9B0BCF` / `981A646F1058DFA3D09CF7EAAE1EAC5D64709B4F1ADB8B62B0F743979091EDE7`，安装器为 `7606E51E05AAD5CF10F3CDAC6EA04152E0E17E904746C206A06EE0BF1C3888BC`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此D组更新为1/5，0.9.1更新为16/25=64%；D2品牌包装、D3自然语言批量字幕修改、D4多比例排版验收和D5预览/烧录逐条对齐仍保持未完成。

2026-08-26 第二十五个纵向切片（D2闭环并正式勾选）：新增 `ass-brand-package-v1`，把“清爽科技/温暖人文/高对比资讯”三种固定设计语法冻结为 `media.visual-effects` 中的 `brand-package` 操作，继续复用同一主进程持久任务、EDL、项目胶囊、恢复、撤销和统一视觉导出质量门，没有新增工程化设计面板。用户可一次说出完整标题、章节条时间、人物姓名与身份、角标和片尾，也可只请求其中任意一项；什么内容都没给或人物缺身份时只追问缺失信息，咨询、否定和举例文本不执行。所有文本进入内部ASS前转义反斜杠与花括号，标题顶部居中、章节条左上、人物条左下、角标右上、片尾居中，时间线按原片时长有界编译，3秒以下拒绝强塞。交付不只检查ASS参数：分别在五类冻结时刻读取源片与成片32×32灰度帧，用独立屏幕区域像素差证明元素真实出现；首轮红测发现章节/人物检测框与ASS 7/1对齐区域不一致后，根据真实变化像素包围盒修正区域，未降低0.004门槛。完整品牌夹具最终标题差0.0673、两章0.0087/0.0094、人物0.0051、角标0.0537、片尾0.026，五类均可见；安装候选从真实对话自动入队，质量100、原视频哈希不变，人工五帧拼图确认互不遮挡。最终完整 `pnpm check` 为975 tests、975 pass、0 fail、0 skip，安全扫描current520/history2078/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为 `DDC84E4F28D1DD93E0709E52FDDF2A27E5FFB7BE48D9FE8936D09C0FF0BB8A3F` / `F3CB26F9F09B08E2C2768DF9F177BAA66F7FBA09A424D5771A9F45AB25555CB4`，安装器为 `E7FBF1D58103EAB940B3CDC9111E1E3861DE7458BE49554008AC820D5CB5D9D8`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此D组更新为2/5，0.9.1更新为17/25=68%；D3自然语言批量字幕修改、D4多比例排版验收和D5预览/烧录逐条对齐仍保持未完成。

2026-08-26 第二十六个纵向切片（D3闭环并正式勾选）：新增 `ordered-subtitle-transform-v1` 与 `media.transform-subtitles`，把自然语言批量改字、合并、拆分、整体提前/延后、翻译和换风格冻结到同一EDL与主进程持久任务，沿用项目胶囊、恢复、撤销、统一审批和模型路由，不再让用户分六次操作。所有结构操作绑定源SRT原始序号：改字可一次最多50条，合并/拆分各最多20处且范围不得重叠；拆分必须给出位于原条目内部的明确秒点与两段文本，没有逐词边界时只追问，不按字符比例猜时间。结构变换后再统一调时、翻译和套样式；英译中优先复用本机OPUS-MT，其他方向继续使用冻结云端路由与既有上云同意，恢复时不重复付费调用，而是从源字幕重算确定性结构并核验成果条数、每条时间、目标语言和ASS预设。强调/简洁/纪录片三种样式输出独立ASS，用户文本的反斜杠和花括号继续全角转义；无样式请求保持SRT。真实安装候选在一个任务中完成改字1、合并1、拆分1、整体提前0.5秒、本机英译中和强调风格，5条源字幕仍形成5条成果，精确结构、中文译文和Impact样式全部matched，质量100且源SRT哈希不变。最终完整 `pnpm check` 为981 tests、981 pass、0 fail、0 skip，安全扫描current523/history2091/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为 `EC629F160419B1D24D82928801737D5D932ADAD167CE17EDDA21DA3C8D6C1775` / `EC118975F162B5535D58C42A83CD34673873949E92A27BDD8E75339445BABBC1`，安装器为 `8221F129012847822572F73D66B48EA536E17D6B1A8E416567A7A8EA6BAD765A`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此D组更新为3/5，0.9.1更新为18/25=72%；D4多比例排版验收和D5预览/烧录逐条对齐仍保持未完成。

2026-08-26 第二十七个纵向切片（D4闭环并正式勾选）：新增 `responsive-ass-layout-v1` 与 `media.subtitle-layout-variants`，用户可从主对话一次生成横屏16:9、竖屏9:16、方形1:1在360p/480p/720p/1080p/1440p中的最多6个ASS布局；本切片不重复B2主体构图，只交付可供对应画布预览/烧录的响应式字幕文件，继续复用EDL、主进程持久任务、项目胶囊、恢复和撤销。字号按画布短边约5.2%计算；每条字幕最多2行，按空格/标点优先断句，超过容量时要求先用D3给真实秒点拆分，不按字数猜时长。每个profile从真实视频秒点生成目标比例裁切帧，比较顶部/底部画面复杂度自动选安全区，再恢复原PTS渲染ASS并用目标区域像素差核对位置。首轮红测因抽帧PTS被重置到0导致所有字幕尚未激活、像素差为0；修复为渲染前恢复原视频PTS，门槛0.004不变。真实安装候选生成横屏360/720p、竖屏360/720p、方形360/720p共6个布局：字号比例0.0514–0.0528，全部最多2行、断句matched、遮挡safe、位置matched；像素差依次0.0369/0.0313/0.0497/0.0448/0.0791/0.0665，质量100，视频和SRT哈希均未变化。最终完整 `pnpm check` 为986 tests、986 pass、0 fail、0 skip，安全扫描current527/history2104/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为 `1FC65A513D147892B4E05027A41ACCD718EEFD570738C5E496BF959702469EF1` / `2A6E1D3E116D433ECFC3F0129F46F45FA25CCFC04131DF88EADDCA56EB96D412`，安装器为 `9FD69C5B8D5581AC6FC25C8B0ACB084F250CB9F63B0FAAD088B0267CC57286C2`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此D组更新为4/5，0.9.1更新为19/25=76%；D5预览与最终烧录逐条对齐仍保持未完成。

2026-08-26 第二十八个纵向切片（D5闭环并正式勾选）：新增 `single-render-subtitle-preview-burn-v1`，关闭“HTML5/VTT预览一套、FFmpeg/libass导出另一套”的错位通道；硬字幕任务在编码前冻结字幕字节、libass滤镜和全量条目账本，账本逐条覆盖文字、起止时间、换行、样式与位置字段，支持SRT/VTT/ASS/SSA，任一字段或渲染参数在处理中变化都会在原子交付前拒绝。完成后播放器预览不再重建字幕，而是直接播放最终交付的同一冻结成片；预览路径、交付路径与成果SHA-256必须完全相同，每条preview/final cue哈希也必须一致。冷启动恢复会重新生成同一账本并复核现有成果，质量门新增`SUBTITLE_PREVIEW_BURN_PARITY_FAILED`硬失败。安装态真实4秒音画夹具含3条字幕，其中一条双行；全量条目matched、sameArtifact=true、质量100，三条活跃时刻相对源片像素差为1.3008/0.8350/1.4209，字幕空白时刻仅0.1016，视频与字幕哈希均未变化。验收首轮还准确暴露路径授权边界：源码目录夹具未模拟用户打开时在入队前被安全门拒绝，脚本修为先走统一打开/拖入授权，未削弱产品路径门。最终完整`pnpm check`为990 tests、990 pass、0 fail、0 skip，安全扫描current530/history2117/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为`66B3FF7D293F8D2CE0844001CF749CF497C0C900BB71603D77615D0B77DA3C95`/`53B06E63BFEBEB51441033628D4600D9A510ADE485F1F60EBC40E958D4AB45A3`，安装器为`ACB02C10238D3FA1DD1BB8F8A94755BDDB17C82876238C8463C9EF263998FAF4`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此D组5/5闭环，0.9.1更新为20/25=80%；下一组按总路线进入E1 AI补镜头/旁白/配音/音效的审批、来源标注与真实交付。

#### E. AI 素材、个人风格与批处理（5/5）

- [x] 缺镜头时可在明确上云/付费批准后生成补镜头、旁白、配音或音效，并标注 AI 生成来源。
- [x] 用户可说“以后这类视频都按这个节奏、字幕和响度处理”，保存为可查看、可修改、可停用的个人编辑 Skill。
- [x] 同一编辑方案可安全批量应用到多条素材，每条独立验收和失败隔离。
- [x] 编辑 Agent 仍共用统一模型路由、审批、预算、运行账本和恢复协议，不建立第二套隐藏执行器。
- [x] 用至少 20 个公开/自有授权样本完成专业人工复核、安装态回归和性能/成本报告。

2026-08-26 第二十九个纵向切片（E1闭环并正式勾选）：新增`ai-generated-asset-bundle-v1`与主进程持久任务`creative.asset-bundle`，把补镜头、旁白、配音和音效收敛为一次自然语言任务，不再让用户进入旧创作面板分别点四次。明确请求先在本机编译冻结素材种类，审批对象消费前模型/生图调用严格为0；只有统一`paid`审批通过后才把去除本地路径的文字要求发送给冻结云端路由，源视频字节从不上传。补镜头采用云端原创关键画面加本机轻微推镜生成可剪辑MP4；旁白由工作模型生成TXT；配音明确标为`ai-text+system-speech`，不冒充云端神经声线；音效由模型给出受限声音设计参数后用本机确定性合成，标为`ai-designed+deterministic-synthesis`。四项成果与`AI生成来源.json`逐一绑定SHA-256、provider/model、生成方法和媒体证明；部分成果或应用中断恢复时复用计划与确定性文件名，模型、生图、配音和FFmpeg均不重复调用。质量门新增审批、种类、来源、真实媒体、源媒体不上云和恢复零重复六类硬证据。安装态在无可用真实云Key的边界下使用仅限`127.0.0.1`环境变量的受控OpenAI兼容端点，仍创建并消费同一付费审批对象，不改变正式路由；生成真实3秒1280×720补镜头、55字节旁白、4.843秒非静音配音（sample peak -4dBFS）和1秒非静音音效（-28.5dBFS），4/4成果及来源清单哈希匹配、sourceMediaUploaded=false、恢复额外调用0、质量100，界面自动预览补镜头且不再铺满绝对路径。最终完整`pnpm check`为995 tests、995 pass、0 fail、0 skip，安全扫描current534/history2131/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为`9DBDC90D6C2BC32100767F83AF5F75766D99F243E4DD56E6538292BD28681D28`/`C9FB232EDF1436E11ABB1A5B28F94F5869AB4D1966B98BCF9AE581B42D2342BB`，安装器为`442A99295C1806E920CB1474FD5BCD91AF3F3D021128C6D0E801965EE9AD99FB`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此E组更新为1/5，0.9.1更新为21/25=84%；下一项E2为可查看、修改、停用的个人编辑Skill。

2026-08-27 第三十个纵向切片（E2闭环并正式勾选）：新增`PersonalEditSkillStore`与`personal-edit-skills-v1`，用户可直接说“以后都按快节奏、纪录片字幕和-18 LUFS处理，保存为知识口播”，也可在主对话查看、修改、停用、重新启用或设为默认；没有增加新的工程面板。Skill只保存节奏`fast/balanced/restrained`、字幕`clean/impact/documentary`和-24至-10 LUFS响度三类可解释偏好，不保存文件路径、凭证或完整FFmpeg命令；主文件原子写入并保留最后一个有效备份，损坏主文件保留取证快照后恢复，未知更高schema拒绝降级覆盖。只允许一个启用Skill作为自动默认，且只在用户以后真正发起卡点、字幕或配乐/音频编辑时注入对应字段；当次明确说出的节奏、字幕样式或LUFS永远优先。每次应用把Skill id/name/revision/digest/fieldsApplied冻结进EditDecisionList质量字段，入队前再与当前Skill核对；已修改或停用的旧计划必须重新规划，任务一旦入队则按冻结计划恢复。安装态完整执行保存→查看→修改→停用（规划无Skill）→启用（规划含Skill）→应用重启→真实配乐：Skill revision4与digest跨重启一致，-18 LUFS进入冻结决策，源视频/音乐哈希不变，成片388,699字节、质量100。首轮验收还发现旧质量门把正常“音乐文件全段”误判为缺时间线，现按有选段核对箭头、无选段精确接受全段回执，未降低声音、响度与统一导出门槛。最终完整`pnpm check`为1000 tests、1000 pass、0 fail、0 skip，安全扫描current537/history2151/packaged5均无发现；候选与桌面安装态EXE/ASAR SHA-256一致，分别为`4CF2887B2ECE12816C5195DE91A54F782F0B65116C2CECF704C9A13A73E75010`/`06FF929F43F6304827B9A37F0D97E8BB46E42F380EAB2A8ADA42AB9DF0A4C5EA`，安装器为`45B79667DEE688A17FD5789BD3BFF4B581E114CA70E7476DCB337A0831193109`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此E组更新为2/5，0.9.1更新为22/25=88%；下一项E3为同一编辑方案安全批量应用、逐条验收和失败隔离。

2026-08-27 第三十一个纵向切片（E3闭环并正式勾选）：新增`independent-media-edit-batch-v1`与主进程持久任务`media.batch-edit`。用户一次拖入2–20条视频并说“全部保留第1秒到第3秒”等明确指令，主进程为每条来源生成等价EDL并对完整计划计算SHA-256；支持保留/删除时间段、配乐和专业视觉效果，混入不同操作、计划被修改、来源或依赖指纹变化均在写文件前拒绝。每条素材拥有独立来源快照、计划输出、项目版本、质量100门和稳定失败对象；某条越界或处理失败不会中止后续素材，也不会把失败项的路径列入可交付成果。每完成一条立即写检查点，重启只继续未进入终态的项，已经成功或隔离失败的条目不重复执行。总质量门不再用“全部都成功”冒充批处理质量，而是要求全部条目终态、成功成果格式与逐条质量匹配、失败项无成果且有稳定失败码，诚实的部分成功仍可得到100分。安装态使用4秒、2秒、5秒三条真实H.264/AAC视频执行同一“保留1–3秒”方案：第一、三条成功且逐条质量100，第二条以`MEDIA_RANGE_OUT_OF_BOUNDS`隔离，交付2项、失败1项、源哈希全部不变；应用重启后任务attempts不增加、`repeatedCompletedItems=0`。最终完整`pnpm check`为1003 tests、1003 pass、0 fail、0 skip，安全扫描current542/history2165/packaged5均无发现；桌面安装位与候选ASAR SHA-256同为`2C7313ACC6543F02438DACC489F279C9F4CAAEB6E067D6794D7C6DB57BD7B04C`，安装位EXE为`5EBBBE43605179F2C674A9679A96115E538E17CC5CFBF43C09C7490E5444F76F`，安装器为`5D5CB1A0A086EC9FEBD75BA284E4BA6653E5CD476DE77BC0595B598DBFDAB17D`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此E组更新为3/5，0.9.1更新为23/25=92%；下一项E4为确认所有编辑Agent共用统一模型路由、审批、预算、运行账本和恢复协议，清除任何旁路。

2026-08-27 第三十二个纵向切片（E4闭环并正式勾选）：新增`shared-media-edit-governance-v1`，把精确剪辑、删除、配乐、多轨、音频修复、节拍、拼接、视觉效果、智能构图、画面修复、多版本、跨素材、字幕烧录/封装/调时/翻译/校对/变换/布局和批量编辑共20类持久任务放入同一治理注册表；新任务在入队前冻结任务类型、决策类型、执行注册表、模型路由、规划模型证据、审批范围、运行时与预算摘要并计算digest，篡改任一字段都拒绝恢复。所有执行统一由`registerGovernedMediaEdit`包装`PersistentTaskRuntime`，每次只允许1轮、1次主执行器调用并记录`AgentRunLedger`；30分钟至6小时的冻结时间预算会真实Abort子任务，不是界面装饰。云端字幕翻译保留原生同意与本地回退，同时把同意结果转成绑定任务specHash的一次性`cloud`审批对象并在调用前消费；模型配置变化仍由冻结路由拒绝。E3曾复制的保留/删除/配乐/视觉效果分支已移除，单条与批量现在共同调用`MediaEditExecutorRegistry`，治理表明确`hiddenExecutor=false`。质量门新增`EDIT_GOVERNANCE_RECEIPT_MISSING/BYPASS`硬失败，要求治理digest、审批状态、预算、账本终态和已验证主进程步骤全部匹配；任务中心同步显示预算与治理回执。旧版已排队任务可由包装器瞬时生成兼容治理合同继续恢复，不修改旧spec或绕过原specHash。安装位以4秒/2秒/5秒三条视频复跑同批保留1–3秒：2成功、1越界隔离、总质量100，治理`runStatus=completed`、`toolCalls=1/1`、`verifiedStep=true`、`hiddenExecutor=false`，重启digest一致且零重复。全部测试为1008 tests、1008 pass、0 fail、0 skip，安全扫描current545/history2183/packaged5无发现；桌面安装位与候选ASAR SHA-256同为`B1CEA8BDA81051230AEDA918CF99398F99A9D5CF2BD3FD25937E189F66E8F6B6`，安装位EXE为`E6EB8B15A0AE0C6330E605F6C86592067CA6DDD3AFE3B662263B66C06BC40936`，安装器为`9F27FFC624BDB01DAA2CAC4205C69340FAAD341843E9B546562900838FD5BB14`；主程序与安装器仍为`NotSigned`，不冒充稳定公开版。至此E组更新为4/5，0.9.1更新为24/25=96%；下一项E5为至少20个公开/自有授权样本的专业人工复核、安装态回归和性能/成本报告。

2026-08-27 第三十三个纵向切片（E5闭环并正式勾选）：新增`agentplay.professional-corpus-e5`公开可复现验收合同，现场生成20个自有授权H.264/AAC样本，按精确保留、删除片段、配乐、综合视觉和字幕烧录五组各4条覆盖640×360横屏、360×640竖屏、512×512方形和854×480宽屏；4个批量任务与4个字幕任务全部从当前桌面安装位执行，20/20质量100、源哈希不变、治理步骤已验证，8个任务重启后零重复。技术回执与人工判断分离：脚本先生成`manualReviewStatus=pending`、逐样本探针、来源哈希、治理、耗时和五张接触表；Codex逐张审查源/结果对照后，再对20条分别填写画面连续、操作匹配、无伪影和文字说明，finalizer只有20/20全部明确pass才生成最终回执和性能/成本报告。接触表确认横/竖/方/宽四种构图保持或按要求变换，9:16综合效果无拉伸/意外黑边，黄色顶部字幕在竖屏自动换行且不溢出；配乐组视觉不变，操作成功由解码PCM、统一声音门和quality100佐证。首轮综合视觉4/4被统一门以`INVALID_SAMPLE_ASPECT_RATIO`拦截，定位为crop/zoompan组合末端未显式归一SAR；在最终滤镜追加`setsar=1`并增加真实ffprobe红测后复跑20/20通过，未降低门槛。Intel i7-8550U/8线程、32GB机器上总执行208054.4ms，P50 5506.9ms/样本、P95 36077.2ms/样本，处理114.5秒源素材、产出10282526字节；本轮只走本机确定性执行，cloudCalls=0、usageTokens=0、估算API费用$0，电费明确未测，不把本机资源或订阅套餐冒充完全免费。最终完整`pnpm check`为1011 tests、1011 pass、0 fail、0 skip，安全扫描current550/history2214/packaged5无发现；桌面安装位与候选ASAR SHA-256同为`A9910ED13A9854010E534E692AEA9940344C38CE3199501156BE469EE15128C4`，安装位EXE为`FEEB06F2817D48EA4D58FE2BD755BCE6E8A5C91F98D7D7DC875826603842E4B8`，安装器为`F6DD2CAFA8304B673F13205AA5E769440F8F84F5ABFCF34E4A16F2A3BF557E61`，仍为`NotSigned`。至此E组5/5、0.9.1固定分母25/25=100%闭环；GitHub推广研究与执行顺序另见`docs/GITHUB_GROWTH_PLAN.md`，签名和Stable发布仍保留原边界。

0.9.1 不以复刻剪映全部界面为目标。主入口仍是对话；只在需要核对时显示轻量时间线、轨道和前后对比。完整自由拖拽 NLE、未经授权的商业音乐下载、自动公开发布和不可解释的全自动删改不进入本版本。

### 0.9.2 以后候选池（暂不计完成率）

- 系统级实时字幕与跨应用悬浮翻译。
- 社区插件/Skill 浏览、权限审查和一键安装目录。
- 手机采集伴侣与跨设备项目继续。
- 更完整的受控电脑操作 Agent。

以上候选只有在 0.9.0/0.9.1 主线稳定或出现真实用户证据时才进入正式版本，不并行铺开。

## 0.8.0 已有能力与遗留复验

最终交互目标（用户已确认）：一个“打开”入口能打开所有文件，一个 AI 对话窗处理所有问题；功能能藏在技术后面就绝不暴露成面板和按钮，首页保持播放器的简洁。统一对话窗 v1 已落地：AI 助手面板内置 📎 全格式打开（视频进播放器、文档变授权附件）、附件驱动的文档任务内联执行、云端模型逐次授权、语音同路由、结果文件可点击。拉片能力已收编进对话流：对当前视频说“拉片/深度解剖/分析这个视频”即内联执行（字幕证据＋覆盖全片的关键帧→专业两部分解剖→报告另存 Word/PDF/MD/TXT/PPT，可取消、走同一条云端授权闸），并修复了云端授权“继续执行”静默失效的缺陷。后续增量：菜单精简、对话级多轮任务、更多真实题材的报告质量标定。现有分离面板在收编完成前保留，不再增加新独立入口。实现手段坚持“超级智能化”：可选组件（本地模型、OCR、格式转换引擎、更大模型等）一律由应用按任务自动下载、校验、安装，沿用组件包清单+SHA-256+断点续传模式，不让用户自己找；模型接入做到粘贴 Key 即自动识别、测速、列模型。桌面安装程序之外，调研 Web/PWA 版承接 AI 对话与文档处理（浏览器内完成，免安装、免代码签名），播放、本地AI推理和系统集成为桌面专属能力。

首个可用闭环已经进入源码：

- 文字输入和语音输入共用同一套任务流程；
- 受控选择文件，原文件默认不覆盖；
- 文本/DOCX生成与基础转换；
- XLSX/CSV清理、去重、明确公式写入，以及自然语言公式规划；
- PPTX新建；
- PDF合并、按页拆分、文字层提取（无文字层的扫描件会明确提示待 OCR）；
- 云模型发送文件正文前逐次授权；
- Windows资源管理器独立“用 AgentPlay 智能处理”右键动词，与“播放”动词分开，文档不再误入播放器；
- 结果文件、历史记录、取消入口和自动化回归测试。

该阶段仍需继续深化：

1. DOCX已有复杂版式的无损修改：确定性查找替换（跨run文字）、修订模式（w:ins/w:del）、锚点/序号段落插入、批注、文末追加均已本地无损落地（样式/表格/图片/页眉页脚不动）。【07-29 已通过 XML 逐字节 + Word COM 实开双重验收】【08-04 行内样式全保真与段落级样式继承完成：跨 run 命中时未触及 run 与 run 间内容逐字保留、替换文字继承命中起点 run 的 rPr、修订按 run 分段留痕可逐段还原、插入/追加继承锚点段落样式（剥分节符）；命中图片/断行等原子 run 自动退回折叠老路不丢内容；16/16 docx 测试 + Word COM 实开验收（修订2/批注1/内容全对）】
2. 已有PPTX的页面级编辑：整稿/单页文字替换、删除指定页（关系与备注页全套清理）、新增页（沿用现有母版版式、标题+要点、指定位置）已本地无损落地（母版/版式/主题/备注/媒体逐字不动）。【07-29 已通过 XML 逐字节 + PowerPoint COM 实开双重验收】【08-04 图表与动画参数化编辑落地：图表标题改字、按类别名改数据点（缓存+嵌入式工作簿双向同步）、按页/整稿删除动画；找不到图表/类别/动画如实报错不碰原文件；13/13 pptx 测试 + PowerPoint COM 实开验收（图表1/动画清零）。动画类型级改写（如"改成淡入"）仍未支持，需要时按预设 timing 树另起增量】
3. 扫描PDF OCR：无文字层 PDF 已自动回退到 Windows 系统 OCR（应用内 Chromium 按页栅格化+WinRT 中文识别，零下载零配置），提取结果标注扫描件与误差提示。【08-04 高精度 OCR 组件落地：PP-OCRv4（det/rec/cls ONNX，@gutenye/ocr-node MIT 管线 + onnxruntime-node，组件包 rapidocr-pack-v1 自托管 GitHub Release、SHA-256 校验、应用内一键下载），扫描件链路高精度优先、WinRT 兜底，真实模型中文识别测试通过】【08-13 新增 Unlimited-OCR 可选外部服务框架：默认关闭、不随包下载约 6.7GB 权重；高级设置连接与健康检查、分页/重复质量门、失败回退、远端 HTTPS + 逐任务云审批、持久任务路由冻结与统一 Agent 工具入口已落地。当前机器未完成真实 GPU 推理，只标记试行，不冒充已验收】；表格结构恢复、双语重排和可靠脱敏见第 5 条。
4. Excel公式：本地安全子集引擎（白名单函数、零eval）已落地——写入前拦截循环引用（含区域覆盖目标列），写入后按真实数据重算抽验并报告样本值与错误行（如#DIV/0!），不支持的函数（如VLOOKUP）写入但明确标注未抽验；联网公式、外部工作簿公式和DDE式危险公式按安全红线拦截（跨工作簿引用为有意拦截，不开放）。【07-29 已经 Excel COM Calculate 重算验收】【08-04 图表与透视表落地：指令含柱状图/条形图/折线图/饼图或透视表即路由 spreadsheet-edit，另存输出后由本机 Excel COM 确定性追加图表页与透视表页（宏禁用、只动输出文件、无引擎如实报错），COM 实测图表部件+标题与透视缓存字段全部写入；已知边界：ExcelJS 读写会丢源文件已有图表/图片（库本身限制），含图表工作簿做清理/去重前需在摘要中明示】
5. 扫描件管线已闭环：无文字层 PDF 自动走 WinRT OCR（此前已接入阅读链）；扫描表格恢复（词坐标聚类→XLSX）、文档脱敏（手机号/身份证/银行卡/邮箱掩码）、双语重排（段落中英对照 DOCX）均已落地并实测；更高 DPI 栅格化与小语种识别仍需继续。
5. DOCX/PPTX/XLSX与PDF之间的高保真转换：→PDF 方向已落地——指令含"高保真/原样/保真"即调用本机 Office 引擎（Word/Excel/PowerPoint COM，宏强制禁用、超时只清理新进程），未检测到引擎时明确故障关闭并提示普通转换。【08-04 PDF→DOCX 反向高保真落地：放弃 Word COM Reflow 路线（本机实测 Word/WPS COM 均无法程序化打开 PDF——Documents.Open 静默空文档，属信任中心策略限制），改走本地版式重建：文字层按坐标聚类还原行/段落、字号映射标题、粗体尽力识别、分页保留，不依赖 Office 全平台可用；图片/复杂分栏不还原与扫描件（指引 OCR）均如实告知】
6. 多文件联合任务：已落地——指令提及两种以上格式且有"和/+/一套"等成套意图时，一次模型调用产出成套 JSON（Word报告/Excel分析/PPT汇报/PDF交付/MD/TXT 任意组合），各格式确定性渲染并统一命名"源名-AgentPlay处理版-报告/分析/汇报/交付"，历史记录全套；跨文件数据一致性校验仍需继续。
7. Windows正式安装包中的真实界面、文件重开和大文件压力验收。【08-12 本轮已完成正式安装位真实视频烟雾：1920×1080 载入、普通模式控制栏常驻、影院静置隐藏、鼠标唤醒、暂停常显、Esc 还原与统一助手聚焦全绿；更大文件与更多编码继续滚动扩样】
8. 本地AI组件改为应用内下载：只保留标准版一个安装包，模型接入中心提供带进度、断点续传和SHA-256校验的组件下载（代码与自动化测试已完成）。【已完成：组件包 local-ai-pack-v1 已于 07-19 上传 GitHub Release（模型+运行时+清单三件齐全），08-04 真实下载验收通过——按清单 URL 实拉两件资产，SHA-256 与 electron/local-ai-pack-manifest.js 逐一相符】

## 播放与系统集成复验

1. 用本轮新安装包再次实测Windows资源管理器“打开方式 → AgentPlay”，覆盖首次启动、已有实例、中文路径、空格路径和多种视频编码；同时实测“用 AgentPlay 智能处理”右键：单选、多选、文档进入工作台而非播放器、原文件不被改动。
2. 真实竖屏、超宽屏和特殊编码视频继续做完整画面、控制栏、自动隐藏与右键菜单回归。【08-05 已回归：竖屏 360×640、超宽屏 1920×400、HEVC(hvc1) 三种片源加载起播正常（含中文字段文件名）、静止 4.5s 控制栏自动隐藏全部达标；右键菜单项与 IPC 链路源码级断言在册（原生菜单 CDP 不可测，交互正确性由人工复验兜底）】
3. AgentPlay品牌迁移继续保留旧`ai-player`用户数据身份，直到专门的迁移版本完成。

## AI高阶能力

1. 多模态拉片和深度解剖需要用实际已配置的视觉模型、字幕和人工镜头证据做端到端质量验收。【07-30 已通过：Agnes 视觉 agnes-2.0-flash + 12 张关键帧 + 无字幕测试视频端到端】【08-04 有字幕长视频成本抽验通过：5分44秒真实口播视频，whisper 本地转写 68s（1463字）→ 场景感知抽帧 27s（20 张顶预算、负载仅 0.5MB）→ agnes-2.0-flash 视觉 53.8s，prompt 2101 + completion 4368 token，报告分镜时间轴与内容准确（MiniMax H3 vs Seedance 评测）；当日端点曾让agnes-2.5-flash拒图，因此保留视觉型号回退】【08-12 专业报告协议固化到底层：正文严格仅“视频讲了什么 / 专业视听拆解与 AI 复刻”两部分，原片观察—专业判断—复刻动作逐项对应；按时长自适应阈值和 12/18/24 帧预算、语义质量门禁不合格自动重写一次、Word 专用排版含真实表格与最多 6 张关键帧。94.144 秒 Lindy 视频实抽 24 帧覆盖 00:00—01:30，WPS 实开导出 3 页无裁切，495 项自动化全绿】【08-24 新证据推翻旧能力结论：agnes-2.5-flash真实JPEG调用HTTP 200，返回文本与reasoning_content；文本和视觉默认统一2.5，2.0仅保留为明确拒图时的兼容回退】
2. AI新镜头、旁白、配音、字幕包装、音乐混剪已具备工作流和渲染入口，仍需按不同模型与失败场景做真实任务验收。【08-04 真实任务验收 4/4 通过：Agnes 生图 1.6MB/49s、Agnes 生视频 2s 新镜头 70s、系统配音、字幕+旁白渲染成片全通；过程中修复生产级 bug——sing-box fake-ip 模式下 SSRF 的 DNS 预检把 Agnes 输出域名（platform-outputs.agnes-ai.space 解析到 198.18.0.0/15 占位段）误拦，生图生视频对 VPN 用户全灭，safe-fetch 与下载链已加 fake-ip 占位识别放行（真假混合仍拒绝）】
3. 双语字幕已落地两档：全文件版“生成双语字幕”（有现成字幕秒级直译、无字幕走 whisper 离线识别+云端翻译，译文排在原文下方，落盘 `源名-AgentPlay双语.srt`）；实时版“实时翻译字幕”（从当前播放位置向前逐批翻译、seek 自动重排、译文到位即叠显在原文下方、译完自动存盘不覆盖已有文件）。翻译引擎双轨：离线翻译组件（OPUS-MT 英译中，transformers.js+onnxruntime，组件包 translate-pack-v1 一键下载、SHA-256 校验）对英文为主字幕纯本地翻译，其它语言自动回退云端模型。无字幕视频的分段实时识别已落地：右键「实时识别字幕」从当前播放位置按 15 秒分段 ffmpeg 抽音 → whisper tiny 带时间戳离线转写（≈1.8x 实时能跟上播放）、识别一句叠显一句、seek 自动追新位置、译完落盘 源名-AgentPlay识别.srt 不覆盖已有文件；无音轨/无语音段按空段跳过不中断，cue 时间戳钳到段边界（ggml-tiny 无 VAD 整段一条）。【07-30 已验收：彩条无音轨 0 句不中断、口播视频 30 秒出 2 条可读中文字幕】句级断句需带 VAD 模型，更多离线语言对（日/韩↔中）按需按同模式加包。双轨精修已落地：实时识别 tiny 出初稿后，ggml-small 组件（465MB，GitHub Release whisper-small-pack-v1 自托管，应用内下载）在位即后台整片精修并落盘替换，实测「三经外」→「三千万」级质量提升（CPU 0.34x 实时故仅后台跑）。【07-30 small 模型实测验证】
4. 网页、新闻和社交网站的全局翻译需要独立的浏览器扩展/受控浏览器方案，不能用桌面播放器按钮冒充全局能力。【08-05 v1 落地：Chrome/Edge MV3 扩展（extension-src/），整页对照翻译，双轨同桌面——离线 OPUS-MT 浏览器内 wasm 推理（同一份模型，内容不出机）/云端 OpenAI 兼容 API（background 中转）；onnxruntime-web 钉 1.24.3 过 q8 兼容坑；Edge 真机验收出对照译文；严格 CSP 站点的 offscreen 迁移与商店上架属下一阶段】
5. 文档工作台和视频工作台打通：视频/网页 → 字幕/正文 → 拉片表 → 报告 → PPT/PDF → 新成片。【07-30 末段闭环落地：拉片（含链接拉片）成功且为 AI 报告后给出「生成重构短片」入口——报告浓缩镜头脚本 → Agnes 逐镜头生视频 → ffmpeg 重编码拼接，成片自动播放并落盘 文档/AgentPlay 输出；实测 2 镜头 3 秒片 861KB 画面内容与主题一致。创作类功能（生图/生视频/重构）在 chat 切本地小模型时自动使用 quickSwitch stash 的云端配置（creativeConfig），不再静默拒绝】
6. 模型无关 Agent Runtime：已落地“问答 / 规划 / 执行 / 自动”四种工作方式、单一播放器工具注册表、轮次/工具/耗时预算、运行账本和真实执行回执。2026-08-13 新增 Electron 主进程持久调度器：执行规范冻结并做 SHA-256 完整性校验，任务、检查点与结果原子落盘；下载、文档、拉片、字幕、创作、批处理、压缩和查重均已迁入，正式安装位冷启动恢复实测从 attempts=1 继续到 2。审批底座统一为 `cloud / paid / publish / delete / credential` 五类对象，令牌用 HMAC 绑定规范、过期时间且只能消费一次。成果质量闭环 v1 同日落地：主进程统一生成 0—100 分、阈值、失败原因和成果文件检查回执；字幕检查时间轴与目标语言，文档检查容器结构，视频检查真实容器签名，拉片同时检查专业语义质量与技术成果；本地安全任务最多自动修复一次并保留前后评分，云端/付费任务不做静默重试，低于门槛按失败关闭。开源插件 / Skill v1 同日落地：第三方扩展只允许 `agentplay-plugin.json + SKILL.md` 声明式包，安装只复制清单明确列出的文件，默认禁用并逐项确认权限；别名工具只能映射现有内置受控工具，继续受统一工具注册表、预算和运行账本约束；旧 JavaScript 插件仅隔离展示，绝不读取执行。模型评测路由 v1 同日落地：日常入口收敛成“智能选择 / 只在本机 / 优先效果”，质量、成功率、速度和模型返回用量只记匿名运行回执，不保存提问、结果或 Key；替代模型至少积累 3 次同类真实任务才可自动接管，价格未知时明确标记未知而不冒充免费；文档、拉片、字幕和创作在审批前选型并冻结身份，恢复执行不得跨供应商换模。模型评测路由 v1.1 同日继续落地：DeepSeek 退役 `deepseek-chat / deepseek-reasoner` 无感迁移到 V4，并分别保留非思考/思考行为；静态清单与旧目录缓存均过滤退役名；上下文、最大输出、缓存命中/未命中官方单价带来源和核验日期进入配置，真实费用按缓存 tokens 估算，路由比较模型单位价格和单次平均费用而不按累计调用次数误判。下一阶段补更多厂商的可核验能力/价格元数据与跨平台实机评测。

## 订阅账号接入【已完成 08-04】

ChatGPT Plus / Claude Pro 这类订阅制账号没有面向第三方的免费 API；已按可行路径落地：复用本机已登录的 Codex CLI（ChatGPT 账号 OAuth 凭证）与 Claude Code（Claude Pro/Max 凭证）经只读子进程完成模型调用，不读/不写用户凭证文件，prompt 一律走 stdin，登录失效给可操作的重登引导。08-04 全链路加固：主对话流式接口补 cli 分支（此前误走网络栈报 Invalid URL）、读取型号走官方 CLI 本地缓存清单、busy 遗留卡死按钮修复、stash 暂存区防订阅配置污染（云端 Key 覆盖事故）、保存后标签即时刷新；CDP 模拟真人全流程实证。Claude Code 登录态过期时需用户在终端自行 `claude auth login`（交互登录，无法代做）。

## 在线内容（08-04 新增，合法源限定）

- 在线媒体库：Internet Archive 公共领域与授权共享馆藏（电影限 feature_films/public_domain_film/Prelinger/classic_tv，音乐限 etree/librivoxaudio/publicdomain），mpv 原生流媒体在线看（边下边播），下载经域名白名单+SSRF 防护落盘进媒体库；archive.org 抖动自动重试；电影条目一键「拉片」与对话窗粘贴链接同一条链路（下载+深度解剖，授权/进度/报告全在对话流）。
- 电子书阅读器：Gutenberg 公版书馆藏（collection:gutenberg）检索 + 维基文库中文公版书（zh.wikisource 官方 API，章节按页序目录、正文按页缓存、礼貌限速），IA（英文为主）与维基文库（中文为主）结果合并、来源徽标区分；维基文库条目直进阅读器。【08-05 真人实证 4/4：搜红楼梦→维基文库→120 回目录→第一回正文】，epub 按 OPF spine 解析章节（txt 兜底），阅读器内置章节导航/字号/上下节；翻译双轨——离线 OPUS-MT 组件免费本地译、云模型精译走逐次授权同意闸，译文按书+引擎+章节缓存零重复消耗，原文/对照视图可切。
- 法律边界写在查询语句里，不抓盗版源、不绕付费墙。

## 其他平台

1. macOS：在真实Mac完成安装、播放、文件关联、系统配音、文档工作台和创意渲染验收。
2. Linux：在真实桌面完成AppImage/deb、文件关联、播放、文档工作台和渲染验收。
3. Android：重新构建APK并做真机文件选择、播放、字幕和权限验收。
4. iOS：需要Mac、Xcode、Apple开发配置和iPhone真机；先建立工程，再做权限、后台音频和文件提供器适配。

## 发布

当前源码候选版本为 `0.8.0`：在 `0.7.6` 的 X/Facebook 统一下载链与“仅下载 / 下载并拉片”双入口之上，完成 AI 原生工作区、字幕任务恢复与专业两部分拉片协议；源码已通过 PR #16 合入公开 `master`，但未创建 `v0.8.0` tag、未上传未签名安装包，也未作为稳定版公开发布。

`0.7.6` 已完成公开 Release、Ubuntu/Windows CI、两层依赖审计、SPDX SBOM、匿名下载和哈希闭环，仍是当前公开稳定版。Facebook 无有效用户 Cookies 时的真实媒体下载继续作为明确限制，不得伪报成功。详细门禁见 [RELEASING.md](RELEASING.md)。

上述变更通过源码检查后，还需要：版本号升级、Windows 标准安装包、已安装 EXE 烟雾测试、安全扫描、公开 Release 资产与匿名下载哈希校验，再发布到 GitHub。每次候选包都必须同步本机唯一安装位、复核桌面唯一“AgentPlay”快捷方式与右键动词，并从安装位完成真实验收；不得用源码或中间包代替。SignPath Foundation 首次申请于 2026-07-23 因公开采用与社区可见度证据不足被拒；补齐公开 Release、签名政策、可验证 GitHub 构建、1 个真实外部 Star 与 1 个真实外部 Fork 后，已于 2026-08-23 重新提交，页面回执为 `Form submitted — Thank you, we'll be in touch soon.`。这只证明申请已送达，不等于获批；当前安装包仍未签名，继续保留 SmartScreen 提示和 SHA-256。获批前不配置 SignPath secrets、不宣称可用可信签名或 winget 分发。SignPath/等价开源签名计划正式获批、候选安装包与安装后 EXE 的 Authenticode 均有效、公开下载回读签名与哈希一致，现已列入 [RELEASING.md](RELEASING.md) 的稳定公开版硬门，不得跳过。
